# DoryAngel Website — Project Memory

## Latest version
**Dori Angel Web.3 — Carousel Blog**

- GitHub repo: https://github.com/dror75p-ops/Doryangel-web-3-carousel-blog
- Live URL (production): https://www.doryangel.com/ — served by **Vercel** since the DNS cutover on 2026-07-07 (apex `doryangel.com` 308-redirects to www; the old Wix site is offline). Use www.doryangel.com for canonicals, sitemap, OG, and any in-app links to this site's own pages.
- Owner GitHub account: dror75p-ops
- Owner email: office@doryangel.com (notification emails go to dror75p@gmail.com via Resend)

### Last changes (as of 2026-08-06)

- **Every host redirect was finally measured from the open internet — and GitHub Pages turned out to be serving a full live duplicate of the site** (2026-08-06, `.github/workflows/host-check.yml`, run 31122031759). Every redirect claim in this project had been verified *indirectly*: the CCR sandbox proxy blocks the live site, so checks went through the Vercel API, which answers for Vercel's **configuration** and returns a `Location` carrying a `_vercel_share=` token — not what an anonymous crawler sees. The probe is a manual, read-only, no-secrets workflow that curls each host from a runner with a Googlebot user agent. **Run it from the Actions tab whenever a redirect claim needs to be true rather than plausible.**
  - **✅ All eight redirects are correct, with clean public `Location` headers and no share token anywhere:**

| URL | Result | Hops |
|---|---|---|
| `http://doryangel.com/` | → `https://doryangel.com/` → `https://www.doryangel.com/` → 200 | **2** |
| `https://doryangel.com/` | 308 → `https://www.doryangel.com/` | 1 |
| `http://www.doryangel.com/` | 308 → https → 200 | 1 |
| `https://www.doryangel.com/` | **200** (`x-vercel-cache: HIT`) | 0 |
| `https://beta.doryangel.com/` | 308 → `https://www.doryangel.com/` | 1 |
| `https://beta.doryangel.com/blog/nyc-housing-court…/` | 308 → **the same path on www** | 1 |
| `/home` | 308 → `/` | 1 |
| `/tenants` | 308 → `/#services` | 1 |

  - **DNS: all three hosts are on Vercel** — apex `A 76.76.21.21`, www `CNAME cname.vercel-dns.com`, beta `CNAME 8406cc71cbb44434.vercel-dns-017.com`. So Vercel's redirect config genuinely applies to `beta`; that was previously an assumption.
  - **✅ RESOLVED 2026-08-07 — Pages is OFF and the duplicate is gone. The finding below is history; do not re-raise it.** The owner set `Settings → Pages → Source` to **None** (the repo page now reads "GitHub Pages is currently disabled"). Re-ran the probe immediately after: `https://dror75p-ops.github.io/Doryangel-web-3-carousel-blog/` returns **404**, `OK — GitHub Pages is not serving this repo`, and the run emitted **zero warnings** — every other host still resolves exactly as the table above. Nave's scheduled 2026-08-07 post published normally afterwards, confirming what the three-layer split predicted: **Pages was never in the publish chain.** Claude Code → repo → Vercel → `www.doryangel.com` is untouched. **Note for anyone re-reading the original finding: use the Branch → `None` → Save control, not the red `Unpublish site` button** — the latter takes the current deployment down but leaves the source configured, so the next push can silently republish it.
  - **⚠️ THE FINDING (now fixed, kept for the reasoning): `https://dror75p-ops.github.io/Doryangel-web-3-carousel-blog/` RETURNED 200.** GitHub Pages is serving a **complete second copy of the site with no redirect**. It is **invisible to the `sc-domain:doryangel.com` property**, so nothing in `daily-series.json`, `rank-history.json` or the host-split card has ever seen it — it is not on the domain. The 2026-07-24 note below said Pages "can be turned off if ever needed"; it never was, and it is still live. Mitigating factor: the pages are copies of this repo's HTML, so their `canonical` tags already point at `www.doryangel.com` — which is probably why the damage has been limited. It is still a second live copy competing during exactly the consolidation window the 2026-08-03 work is trying to shorten.
    - **Do this in order:** open the **`dror75p-ops.github.io/Doryangel-web-3-carousel-blog/` GSC property** (it exists — see the 2026-07-24 note) and read its impressions **first**. Near zero → turn Pages off at repo → Settings → Pages → Source: None. Not zero → it is a real unmeasured traffic source and the decision changes. **Do not turn Pages off before reading that property.**
    - Note the Pages copy is currently **stale** (the `pages build and deployment` runs failed during the 2026-08-06 Actions outage below), so re-check freshness after Actions recovers — a stale duplicate is a different problem from a current one.
  - **✅ CORRECTION — "fix the beta redirect per-path" is DONE and needs no work.** The 2026-08-03 note (still at the entry below) said to fix beta per-path before ever touching that DNS record, implying deep paths were not preserved. **They are:** `beta.doryangel.com/blog/nyc-housing-court…/` 308s to the identical path on www in one hop. The site's third-best page is not at risk. The rest of that note stands — **still do not delete the `beta` record** while it carries impressions.
  - **✅ CONFIRMED LIVE — `/tenants`** now serves `308 → /#services`. That was added on 2026-08-03 and had never been verified against the real host.
  - **Only cosmetic gap:** `http://doryangel.com/` takes **two** hops (http apex → https apex → https www) where every other host takes one. Harmless, and not worth a change on its own.

- **✅ THE 2026-08-03 SERIES CODE HAD ITS FIRST REAL RUN AND PASSED — the open "check the first Actions run" item is now closed** (2026-08-06, run 31128220821, commit `af7311d`). The 08-03 note said nothing there had ever executed against live Search Console, so the stub proved self-consistency and not the contract. It does now: **`GSC series: 478 days on file (backfilled this run), 8 watch terms, 1 change-log entries`**, and `daily-series.json` / `query-series.json` / `annotations.json` were created and committed (30,965 insertions across 4 files). **No `Watch-term series … failed` and no `No watch-term rows` warning** — the `groupType:'and'` trap that would have written `query-series.json` empty every day did not fire. The eight-single-filter design is correct against the real API.
  - **⚠️ The "93% of impressions are address lookups" claim is now definitively dead — do not resurrect it.** `summariseQueryMix()` over all 500 rows measures **address 30 impr / 0 clicks · branded 5 / 0 · commercial 2,443 / 12 · other 818 / 2**. Address lookups are about **1%** of impressions, not 93%. The old figure was an artifact of reading the click-sorted top-25 as a tail sample.
  - **📊 Host split, measured 2026-08-06** (28-day window ending 3 days back): **www 4,309 impr @ 34.0 · apex 1,358 @ 6.2 · beta 1,123 @ 27.1**. Compared with the 08-03 reading (www 2,510 @ 47.7 · apex 1,355 @ 5.9 · beta 68 @ 11.2): **www is improving** (position 47.7 → 34.0 on 72% more impressions), **apex is flat** (1,355 → 1,358, still ~position 6 — the local-pack signature), and **beta jumped roughly 16×**, from 68 to 1,123 impressions. **Watch the beta number** — it redirects correctly per-path (verified above), so this is Google serving more beta URLs, not a server fault.
  - **⚠️ `bronx property management` is drifting the WRONG way: 19.9 → 20.5 → 22.8 → 23.5 → 25.0.** Per PR #276 the decision point is ~2026-08-30: if it has not moved off ~20, the title was not the constraint and the next move is backlinks / Google Business Profile, not more title wording. It is now *worse* than when the title changed. **Do not read that as proof the title hurt** — these are 28-day rolling aggregates sharing 27 of 28 days, exactly the confound the series files exist to remove. **With 478 days of true per-day history now on file, this can finally be answered properly instead of inferred.** That is the first thing to do with the new data.
  - Other figures from the run: GA4 4 sessions today, leads 325 total / 41 in 30d, Clarity 35 sessions per 3 days at 24.29% scroll, Hailey 10/30 = 33% success. Arlo filed **issue #285** ("Add schema markup + FAQ section to blog posts for featured snippets") and the digest delivered.
  - **Minor, do not chase it:** the audit's `Checks:` line always prints `hasBreadcrumbInBlog: false`. That is a **hardcoded literal** at `scripts/daily-audit.js:120` with the comment "checked separately in build-blog.js" — it is not a measurement. Breadcrumb JSON-LD **is** present on blog pages (verified on the 08-06 post). Worth either wiring up or deleting so it stops reading like a regression.

- **🚨 GITHUB ACTIONS RUNNER OUTAGE, 2026-08-06 — and the fix committed for it that morning was aimed at the wrong thing.** From ~16:00 UTC, jobs in this repo were **queued without ever being assigned a runner** and auto-cancelled at exactly **15:01**. Four runs died this way (audit ×3, Nave ×1), plus GitHub's own `pages build and deployment` ×3.
  - **The signature to recognise, so this is never re-debugged as a code bug:** job conclusion `cancelled`, **`runner_id: 0` and `runner_name: ""`**, zero step records, no logs at all. `actions/checkout` never runs, so **no script in this repo can possibly be involved.**
  - **⚠️ COMMIT `e93a6ea` MISDIAGNOSED THIS. Its reasoning is wrong; keep the code anyway.** It read run `31118286414`'s bare "cancelled" as a hanging Google `fetch` in `daily-audit.js` and added per-request timeouts plus `timeout-minutes: 20`. Three things disprove it: run 98 failed identically 26 minutes later on the **same pre-fix code**; run 99 failed **after** the fix merged; and GitHub's Pages build — which contains none of this repo's code — failed the same way. The 15:01 death is **before** the 20-minute backstop, and Nave's workflow has **no `timeout-minutes` at all** yet died at the identical 15:01. The fetch timeouts are still correct hygiene for a real hang — **leave them in, just do not believe they fixed this.**
  - **Proof it was not any repo change, for the next person who suspects one:** the **first** failure ran at `head_sha 3bb600bd`, the *2026-08-05* state of `main`. PRs #283/#284 merged at **17:48**, nearly two hours later. The only commits between the last green run and the first red one were two bot commits — `Auto-publish 2026-08-05` and `Arlo: rank snapshot 2026-08-05` — touching only content and data files. Every `.github/` change since 2026-08-01 is dated **after** the failures began.
  - **Recovery is just a retry.** Nave was re-dispatched unchanged at 21:12 and completed in 3m51s, all steps green.
  - **📌 Queued for later:** run logs now warn that `actions/checkout@v4` and `actions/setup-node@v4` target Node 20 and are being **force-run on Node 24**. Nothing is broken yet; bump both to `@v5` before the forcing becomes a hard failure.

- **Phase 1 audit of the owner's "Website Improvement Master Plan" — two fixes shipped** (2026-08-05, branch `claude/dory-angel-improvement-plan-0pbg8i`). Owner supplied a 7-phase improvement plan; Phase 1 (audit, analysis-only) was run and the owner approved two of the three critical findings. Full audit: https://claude.ai/code/artifact/41204d28-c4cc-4278-9c9d-e614fb8ceae1
  - **The compliance guide's lead form had four unlabelled inputs** (`guides/bronx-landlord-compliance/index.html`) — name/email/phone/property carried a placeholder and nothing else, so a screen reader announced each as "edit text, blank" (WCAG 1.3.1/3.3.2/4.1.2). **This is the same defect fixed on `flat-fee-vs-commission` on 2026-07-27** — both pages are built on the same landing-page shell, the guide was born with the bug in PR #253, and the fix never propagated. Fixed with the identical `.lead-field` pattern so the two forms' CSS is now byte-identical. **When fixing a shell-level bug, check every page on that shell** — there are 8.
  - **⚠️ THE 57 BLOG PAGES NEVER RECEIVED THE HOMEPAGE'S LOAD FIXES.** All four corrections live in `scripts/build-blog.js`; do not undo them by "simplifying" the head or the hero markup:
    1. **Google Fonts moved off the critical path** (preload + onload rel-swap + `<noscript>`), matching `index.html`. It was the only render-blocking request on the page.
    2. **`preconnect` to `fonts.gstatic.com`** — only `fonts.googleapis.com` was warmed, so the connection that actually serves the font files was the one being missed. Plus `preconnect` to `images.unsplash.com`.
    3. **⚠️ The hero is now a real `<img>`, not a CSS `background-image`.** As a background it could not be discovered until CSS was parsed and the render tree built — the latest possible moment for what is the **LCP element on every post**. It now carries `srcset`/`sizes`, explicit `width`/`height`, `fetchpriority="high"` and a document-top `preload`. `object-fit: cover` reproduces the old `background-size: cover` exactly, and `heroImageAlt` moved from `aria-label` to a real `alt`. **Do not revert it to a background div.**
    4. **The three "Continue reading" thumbnails are lazy `<img>`** — they were backgrounds fetching the full-size file into a 160px box whether or not the reader ever scrolled that far.
  - **New helpers `imageAtWidth()` / `srcset()`** rewrite the `w=` param on the Unsplash URL. A URL with no `w=` param yields an empty srcset and the caller falls back to a plain `src`, so a non-Unsplash hero degrades safely.
  - **Verified** in headless Chromium at 390px and 1280px, before vs after: hero box identical (390×240 / 1280×400), the `::after` gradient intact, thumbnails still 160px, and **mobile now selects the 640w image where it previously took 1200w**. All 57 pages checked for leftover `background-image`, render-blocking stylesheet and correct hero markup; `sitemap.xml` regenerates byte-identical.
  - **Pre-existing, NOT fixed, left deliberately:** a hashtag chip in `.hashtag-row` overflows the viewport by ~9px at 390px on every post page (horizontal scroll). Confirmed present before this change. Likely a one-line `flex-wrap` fix in the same generator.
  - **⚠️ Deliberately NOT done — the tax checklist still says 2025.** `tax-checklist/index.html` (title, meta description, H1) and the homepage tool card (`index.html:3213`, "Updated for 2025") all advertise the prior tax year. Owner deferred it. Worth pairing the string change with a content review of the 28 deductions for the 2026 tax year.
  - **Also from the audit, still open (medium priority):** compliance-calendar labels have no `for=` attribute; the compliance guide's meta description is 258 chars (truncated) and the FAQ's is 65 (too short); `tax-checklist` has zero `og:` tags; no JSON-LD on compliance-calendar / pl-dashboard / broker-partner; `/tools/pl-dashboard/` missing from **both** `sitemap.xml` and `buildSitemap()`; `h2→h4` heading skips on four pages; 228KB of inline CSS re-shipped across 8 landing pages with zero cross-navigation caching.

- **✅ RESOLVED 2026-08-05 — the Google Cloud project was restored by the owner and all three grants are live again.** Verified from the data, not the email: `project/seo/rank-history.json` has an unbroken daily snapshot for **2026-07-31 through 2026-08-05** with real Search Console figures (clicks climbing 75 → 85, 4 of 8 watch terms ranking), so there is no outage gap at all. GA4 and the lead sheets run on the same `GOOGLE_SA_KEY` and are in the same state. **The alarm below is history — do not re-raise the 30-day restore deadline as an urgent owner action.** The `daily-audit.js` reporting fixes it produced are still valid and still worth keeping.

- **Rank data went from 4 overlapping aggregates to a real per-day series — and the homepage turned out to be ranking under three hosts at once** (2026-08-03, owner asked "is there enough data to understand what affects the site's position?", branch `claude/site-location-data-analysis-183wcq`). The honest answer was **no**, for a structural reason rather than a volume one, and the fix is in `scripts/daily-audit.js`.
  - **⚠️ THE 28-DAY SNAPSHOTS CANNOT ATTRIBUTE A CHANGE TO A CAUSE — that is why the series files exist. Do not "simplify" them back into `rank-history.json`.** Each snapshot is a 28-day aggregate and each run shifts it by one day, so **consecutive snapshots share 27 of 28 days (96% identical data)**. A one-day move surfaces as ~1/28th of itself, smeared over the following four weeks. That is why `bronx property management` reading 19.9 → 20.5 → 22.8 → 23.5 across the first four snapshots is *uninterpretable*: it is equally consistent with a real slide and with three low-impression days rolling through the window.
  - **The fix is one extra dimension, and it is retroactive.** GSC retains ~16 months and will break any query down by `date`. So `fetchDailySeries()` backfills **480 days of true per-day history on its first run** instead of waiting months for daily snapshots to accumulate. Three new files, all under `project/seo/` (which `.vercelignore` excludes — `content/` does not, and `blog-loader.js` fetches `content/` client-side, so a keyword export there would be public at a guessable URL):
    - `daily-series.json` — per-day clicks/impressions/position, **split by host**, as `[clicks, impressions, position]` tuples (objects would be ~19k lines and undiffable).
    - `query-series.json` — same shape per watch term, filtered server-side to the 8 `WATCH_TERMS`. An unfiltered `date×query` pull is tens of thousands of rows of one-impression address searches. **⚠️ It is deliberately EIGHT requests, one per term, each carrying a SINGLE filter — do not fold them into one call.** The obvious way to do that is a filter group of eight `equals` with `groupType:'or'`, and the Search Console API only accepts `groupType:'and'`: eight equals-filters ANDed together mean "a query that is all eight terms at once", which matches nothing and returns **200 with zero rows**. The file would be written empty every day with no error anywhere — the same silent-success class as Vera's month of skipped Facebook posts. A run where every term comes back empty now emits `::warning::` for exactly this reason.
    - `annotations.json` — the change log, **derived from `git log` each run**, so the backfilled ranking history lands next to a backfilled change history for free. Rank-snapshot commits and `docs:` commits are filtered as noise.
  - **⚠️ ALL FOUR FILES MUST STAY IN THE WORKFLOW'S `git add` LINE** (`daily-website-audit.yml`, "Commit rank history" step). Anything written on the runner and not committed is thrown away with the container — so omitting one means the 480-day backfill is re-fetched and re-discarded *every single day* and no trend ever accumulates. Same lesson as `category-plan.json` and as the reason that step is deliberately separate from the PR step.
  - **The email now leads its trend with 7 days vs the previous 7 — two windows that do not touch.** The 28-day delta beside the big number is kept but is no longer the only trend shown. `sumWindow()` weights position by impressions, not a flat mean.
  - **📊 THE FINDING: the homepage is being served by Google under THREE hosts, ranking completely differently.** Measured 2026-08-03 over 2026-07-04 → 07-31: `https://www.doryangel.com/` **2,510 impressions at position 47.7**, `http://doryangel.com/` **1,355 impressions at position 5.9**, `https://beta.doryangel.com/` 68 at 11.2. Those three are **67% of all site impressions**, so the headline "avg position 29.5" is a blend of three different things and should never be read as one number — the new host-split card in the email says exactly that.
  - **This is NOT a server misconfiguration — verified, do not go re-fix the redirects.** Both `https://doryangel.com/` and `https://beta.doryangel.com/` return **308 → `https://www.doryangel.com/`** (checked against Vercel directly; all three hosts are attached to project `prj_vHgkeapfoabotVYhyJXnKPU0H3y1`). The split is Google still serving URLs it has not yet consolidated, ~4 weeks after the cutover. The rolling-window arithmetic says it is **not** simply pre-cutover residue either: apex impressions barely moved (1368 → 1355) across three snapshots, where days-rolling-out alone would have dropped them by hundreds. **The 2026-07-24 note claiming consolidation was confirmed done was premature.**
  - **⚠️ 71% OF ORGANIC CLICKS LAND ON URLs THAT ARE SUPPOSED TO BE REDIRECTS.** Not just the apex homepage — `/home` (168 impr, pos 4.9), `/property-management` (65, 5.8), `/tenants` (13, 4.2) are **Wix-era paths that only exist as redirects in `vercel.json`**, and 12 of the 17 blog pages in the top-25 are served under `beta.doryangel.com` (including the site's **third-best page**: a New Rochelle post at 324 impr / pos 9.7). Adding it up: 57 of 80 clicks arrive on legacy URLs; the canonical `https://www.doryangel.com/` earns 26 at position 47.7. **Consequence for planning: every on-page change is being applied to the URL that earns a quarter of the traffic and ranks 47th. Consolidation gates the measurability of all content work** — that, not confounding, is the reason to do host work first.
  - **⚠️ DO NOT DELETE THE `beta` DNS RECORD.** An earlier note called it "harmless, delete someday". It currently holds the third-best-performing page on the site; deleting it 404s that page. ~~Fix the redirect per-path and~~ **✅ measured 2026-08-06: the per-path redirect is already correct — beta deep paths 308 to the identical path on www in one hop, so there is nothing to fix here.** Leave the record for ~6 months until its impressions are genuinely zero.
  - **⚠️ THE MECHANISM THAT ACTUALLY ENDS THE SPLIT IS EXTERNAL LINKS, NOT A BUTTON.** Google keeps indexing the old URL largely because ~15 years of citations (Yelp, Birdeye, LinkedIn, Facebook, the Bronx Chamber, NYC landlord directories) still point at `doryangel.com`. Updating the top 15–20 listings to `https://www.doryangel.com/` is the accelerant. Also unverified and high-leverage: **the website field on the Google Business Profile** — GSC counts local-pack appearances as ordinary web impressions, and apex's 1,355 impressions at position 5.9 with 3.2% CTR is the signature of a local-pack result, not an organic one. If that field still reads `http://doryangel.com`, it may be sustaining the split single-handedly. **And `sameAs` in `index.html` carries TWO different `share.google` links — check there is only one live GBP**, because two profiles fragment the 21 reviews.
  - **⚠️ `topQueries` IS TOP-25 BY *CLICKS*, NOT BY IMPRESSIONS — and reading it as a tail sample produced a wrong strategic conclusion on 2026-08-03.** GSC sorts rows by clicks descending, so the stored 25 are ~11 rows with clicks followed by whichever zero-click rows sort **alphabetically** first (numeric strings win, which is why they look like a wall of street addresses). The other 475 fetched rows were discarded. A claim that "93% of impressions are address lookups" was built on that artifact and was **false** — the tail had never been measured at all, and a high-impression commercial query can hide there entirely. Fixed by `summariseQueryMix()`, which buckets **every** returned row into address / branded / commercial / other, plus a `topByImpressions` list alongside the click-sorted one. **Keep all three — they are a set.**
  - **⚠️ THE SITE-WIDE "avg position" HAS BEEN REMOVED FROM THE EMAIL ON PURPOSE. Do not put it back.** It averages three hosts ranking ~6, ~12 and ~48 over a query set that is largely building-address lookups, so it cannot respond to anything the business does. The email reports the **commercial basket** instead.
  - **`fetchHostQueries()` (page × query) now answers the question this note used to say was open** — whether apex genuinely outranks www or the hosts just serve different query mixes. It keeps queries served by more than one host (plus the watch terms) and reports position + impressions per host for each, so "the old URL is holding the position" is now a measurement rather than an inference.
  - **Also fixed:** `/tenants` — a live Wix-era path drawing 13 impressions at **position 4.2** — had no entry in `vercel.json` (unlike `/home`, `/contact`, `/property-management`) and was 404ing. Now redirects to `/#services`.
  - **Verified** by a harness that stubs the OAuth exchange and the GSC API and drives `getSearchConsoleStats()` end to end against synthetic data (72 checks, all passing): the backfill spans 477 days on a cold start and only 11 on the next run; the two 7-day windows provably do not overlap; each host regex refuses to swallow the others; a failing host query still yields a snapshot; and the rendered email contains all three new blocks. **The stub deliberately reproduces two API constraints rather than being permissive** — it 400s on any `groupType` other than `and`, and returns zero rows when a group carries multiple `equals` filters on one dimension — so the empty-`query-series.json` trap above is now a caught regression rather than a comment.
  - **⚠️ WHAT THE HARNESS CANNOT PROVE, and why the `groupType` bug got written in the first place.** The CCR sandbox has no `GOOGLE_SA_KEY` (it is an Actions secret) and its proxy blocks Google and the live site, so **nothing here has ever run against real GSC** — the redirect checks had to go through the Vercel API for the same reason. The stub encodes *my* reading of the API, so it proves the logic is self-consistent, not that the contract is right. **Check the first real Actions run** for `GSC series: N days on file (backfilled this run)` and for any `Watch-term series … failed` or `No watch-term rows` warning before trusting the first committed series.

- **🚨 THE `GOOGLE_SA_KEY` SERVICE ACCOUNT DIED — its Google Cloud project was deleted, and Arlo blamed the wrong thing** (2026-08-03, owner forwarded the daily email, branch `claude/arlo-dangerous-issue-zw5etp`). The email showed "🔍 Search Console not connected yet — 2 minutes to fix" and "🔌 GA4 not connected — add GA4_PROPERTY_ID and GOOGLE_SA_KEY as GitHub secrets". **Both instructions were wrong.** The secrets were set and every grant was still in place.
  - **The real cause, from run 30831236721:** `403 PERMISSION_DENIED — Project #1064037172834 has been deleted (CONSUMER_INVALID)`. The Cloud project that owns `doryangel-arlo@my-project-84420-translate.iam.gserviceaccount.com` is gone, so **all three** of its grants died in the same instant — exactly the risk the 2026-07-31 note warned about. Last good run: **2026-08-02 14:55 UTC** (rank snapshot committed, Hailey 10/30 sessions read). First bad run: **2026-08-03 16:14 UTC**.
  - **⚠️ OWNER ACTION — ✅ DONE 2026-08-05, the project was restored. Kept for the runbook only.** Google keeps a deleted project restorable for **30 days** (so until roughly **2026-09-02**): console.cloud.google.com/cloud-resource-manager → *Resources pending deletion* → **Restore**. Restoring brings GA4, Search Console and the lead sheets back with **no other changes** — nothing needs re-granting. Miss the window and it is a new project + new service account + re-enabling three APIs + re-granting in **three separate places** (GA4 property, the `doryangel.com` Domain property, and each of the 5 lead sheets) + a new `GOOGLE_SA_KEY` secret.
  - **What still works, and why:** Nave's digest survived because `notifyDigestSubscribers()` falls back to the sheet's **public CSV export** (`Sheets API returned 403 → Subscriber list loaded via public CSV → 3/3 subscribers`). Vera, Resend, Clarity, Make and the site itself are untouched — none of them use this key. ~~**Rank history stops accumulating** until the project is restored (`project/seo/rank-history.json` ends at 2026-08-02).~~ **Superseded — the restore landed before any snapshot was actually lost; the file runs unbroken through 2026-08-05.**
  - **⚠️ THREE REPORTING BUGS FIXED IN `scripts/daily-audit.js` — do not undo them by "simplifying" the three cards back into one shape.**
    1. **A failed fetch is no longer reported as an unconfigured integration.** GA4/GSC/leads each return `null` only when genuinely not set up, and `{ error: { kind, message } }` when configured-but-failing — three distinct cards. New `classifyGoogleFailure()` sorts the raw API error into `project-deleted` / `sa-invalid` / `api-disabled` / `access` / `unknown`. **Never print "add the secret" or "add the user" on a live connection** — that is what wasted the owner's morning.
    2. **One red banner replaces three yellow ones when the shared credential is the cause.** The `project-deleted` / `sa-invalid` kinds are fatal *and shared*, so the email leads with a single "this is ONE problem, not three" card naming the account, the project, Google's verbatim message, and the 30-day restore path — and the per-source cards suppress themselves. Three unrelated-looking cards is what made this read as three separate setup chores.
    3. **⚠️ Lead counts no longer render as `0` when every sheet read fails.** A failed sheet contributed 0 to the totals, so the table showed a tidy "0 today / 0 this month / 0 all time" — **a broken credential that looks exactly like a dead business**. When all sources error it now says the counts are *unknown*, and states that leads themselves are unaffected (Make still writes every submission).
  - **Also: the run is no longer a silent green tick.** A dead credential emits `::error::` from both `main()` and the GSC handler; GA4/leads failures emit `::warning::` instead of `console.warn`. Nave's silent `console.warn` on the CSV fallback became a `::warning::` too — it was the only trace of the outage in the publish log. Same silent-success class as Vera's month of skipped Facebook posts.
  - **Verified** by replaying the real 403 through `sendDigest()` in three scenarios (project-deleted / not-yet-granted / all-healthy): the banner appears only in the first, the "2 minutes to fix" and "add the secret" copy appears only in the genuine first-run case, and no zero-lead table is ever rendered on a failed read. The Search Console card also now reads **"access lost"** rather than "not connected yet" once `rank-history.json` proves it used to work.

### Older changes (as of 2026-07-31)

- **The Compliance Calendar moved on-site to `/tools/compliance-calendar/` — the Digest deliberately did NOT** (2026-07-31, owner request, branch `claude/doryangel-tools-migration-57o0jh`, PR #274 merged). The tool was served from the personal GitHub Pages subdomain `dror75p-ops.github.io/Doryangel-preventive-maintenance-schedule.automation/`, so every backlink the documented distribution plan would earn (r/Landlord, BiggerPockets, NYC landlord forums) — plus the tool link in **every blog post's Facebook caption** — credited that subdomain instead of doryangel.com. Page imported from source commit `650bf58`; only URLs were rewritten, the body/styles/webhook are byte-identical.
  - **⚠️ FOUR THINGS A FUTURE SESSION COULD UNDO WITHOUT KNOWING WHY. Read before touching the tool links, the tool gate, or `/tools/`.**
    1. **The DoryAngel Digest page was left on github.io ON PURPOSE — do not "finish the job".** The homepage tool-gate already captures digest signups (with topics) into Make webhook 5549170, so the external page is a redundant *second* signup surface. All `/digest/` links across the site are intentionally still off-site. Note the calendar URL is a **string prefix** of the digest URL, so a naive find-and-replace will silently mangle the digest links — the migration used a negative lookahead (`automation/(?!digest)`) for exactly this reason.
    2. **⚠️ Do NOT change `window.open` in `openToolGate()` to same-tab navigation** now that the tool URL is same-origin and it "looks like" it should just navigate. The handler opens the tool tab **before** firing a fire-and-forget `fetch()` to Web3Forms. Same-tab navigation unloads the page and **cancels that request in flight — silently killing the owner's lead-notification email for every Compliance Calendar signup**, while everything still looks fine to the user. Same silent-failure class as the month of skipped Facebook posts.
    3. **The old github.io page is still live, and that is the safety net.** No redirect stubs were added, so any link that was missed keeps working instead of 404ing. Consequence: the content exists at two URLs. The new page's `canonical`/`og:url` point at `https://www.doryangel.com/tools/compliance-calendar/`, which is the signal that consolidates them — **never point those back at github.io**, or Google reads the on-site page as the duplicate and the whole move is undone. Optional tidy-up: a meta-refresh + canonical stub in the source repo (separate repo, separate PR).
    4. **The sitemap entry lives in TWO places and must stay paired** — `buildSitemap()` in `scripts/build-blog.js` *and* `sitemap.xml`. The generator rewrites that file wholesale on every Nave run, so a file-only edit is silently reverted by the next auto-publish (same bug fixed for the compliance guide in PR #255). Verified at merge time that the generator's output is byte-identical to the committed file.
  - **Incidental fixes that came with the import:** the page's own "Schedule Free Consultation" and logo links still pointed at the **dead pre-cutover host** `dror75p-ops.github.io/Doryangel-web-3-carousel-blog/` (now root-relative), and its Digest card used a relative `./digest/` href that would have 404'd under the new path (now absolute). The page ships its **own** `assets/logo-icon.jpg` — it differs from the main repo's copy, so do not "de-duplicate" it.
  - **No Turnstile on this page**, so no hostname-allowlist action was needed (unlike the Retell/Turnstile cutover failures). `posts-index.json` and `blog/` were deliberately untouched to avoid a 49-page regeneration; one blog post body still links the old URL, which is harmless while that page stays live. **Still open:** `/tools/pl-dashboard/` is missing from `buildSitemap()`'s static list — pre-existing gap, left alone.

- **Arlo now reports Google rankings — and the neighborhood-pages idea was built, then deliberately dropped** (2026-07-31, branch `claude/new-agent-screenshot-xc8kq6`). Prompted by owner screenshots of a competitor ("Glasspath") selling AI local SEO: *"AI agents build 80–800 location pages, optimize your GBP."*
  - **⚠️ WHAT SHIPPED IS ONE FUNCTION INSIDE ARLO. NOT A NEW AGENT.** The first design proposed two new agents and the owner rejected it — *"too many agents already"* — which was the right call and improved the design. Rank tracking is a **fourth data source inside `daily-audit.js`**: Arlo already runs daily, already holds `GOOGLE_SA_KEY`, already sends the email, and `getGoogleAccessToken(credentials, scope)` (`:99`) was already generic over scope. A separate agent would have duplicated a cron, a workflow, an email sender and that helper to deliver one more table. **Count stays at three: Nave, Arlo, Vera. Do not re-propose a rank-tracking agent.**
  - **What it answers:** GSC shows ~63 clicks / 4,731 impressions per 28 days with **"bronx property management" at roughly position 12 — top of page 2**. Position is the bottleneck, and nothing measured it; the standing "re-pull the export in 2–4 weeks" was never going to happen by hand. The email leads with that number and its day-over-day delta, plus a watch-list table and top pages by impressions.
  - **⚠️ THREE THINGS THAT WILL BREAK IT IF CHANGED.** `siteUrl` must be the **`sc-domain:doryangel.com`** form — the owner has a Domain property and a URL-prefix value 403s. The window ends **3 days back** because GSC finalises late. And it uses the **official Search Console API** — never scrape Google results for rankings, that breaches Google's ToS.
  - **⚠️ THE HISTORY IS COMMITTED BY ITS OWN UNCONDITIONAL WORKFLOW STEP.** The existing commit step in `daily-website-audit.yml` only fires when an auto-task ran — the minority of days — so a snapshot riding along with it would be discarded with the runner on every other day and the trend would never accumulate. Same lesson as `category-plan.json`. **Do not fold that step into the PR step.**
  - **History lives in `project/seo/rank-history.json`.** `project/` is in `.vercelignore`; `content/` is **not** and is fetched client-side by `blog-loader.js`. Putting it under `content/` would publish the full keyword export at a guessable URL.
  - **Fails soft everywhere:** missing key, malformed key, 403 and empty windows all warn and return, leaving the rest of Arlo's report intact. A 403 renders a yellow card naming the exact `client_email` to paste — that card is how the address above was obtained in the first place, so leave it in.
  - **⚠️ ALSO FIXED IN THE SAME PR — Arlo used to report the digest as sent even when Resend rejected it.** `sendDigest()` called `resend.emails.send()` and never inspected the result, and `main()` printed `Digest sent to …` unconditionally. **The Resend SDK does not throw on an API error — it resolves with `{ data: null, error: {...} }`**, so any rejection produced a green run and a log line claiming delivery. Same silent-success class as Vera's month of skipped Facebook posts. Now a rejection or a missing message id is an `::error::` naming the reason, and the log reports the Resend message id. (Delivery itself was fine — the owner was receiving the emails. This was a latent trap, not the cause of a live outage.)
  - **✅ CONNECTED AND LIVE 2026-07-31.** First real snapshot committed to `project/seo/rank-history.json` (427 lines) on run 30631292958. The connection details, so nobody has to rediscover them:

| Thing | Value |
|---|---|
| Service account | `doryangel-arlo@my-project-84420-translate.iam.gserviceaccount.com` |
| GCP project | `my-project-84420-translate` |
| Search Console property | **`doryangel.com` Domain property** (NOT the `https://www.` URL-prefix one) |
| Permission granted | **Restricted** (enough for `searchAnalytics.query`; Full only needed if it ever submits sitemaps) |
| API enabled at | `console.cloud.google.com/apis/library/searchconsole.googleapis.com?project=my-project-84420-translate` |
| Secret used | `GOOGLE_SA_KEY` — the **same** service account already reading GA4 and the lead Sheets. No new secret was added. |

  - **⚠️ THE SAME SERVICE ACCOUNT NOW HOLDS THREE GRANTS** — GA4, the three Google Sheets, and Search Console. Revoking or rotating `GOOGLE_SA_KEY` breaks all three at once, and the Sheets grants are per-sheet shares that are easy to forget.
  - **⚠️ BOTH HALVES ARE REQUIRED AND THEY LIVE IN DIFFERENT PLACES.** Adding the user in Search Console is not enough on its own — the **Search Console API must also be enabled in the GCP project**. Missing either one produces the same 403, so if this ever breaks, check both before debugging code.
  - **📊 REAL BASELINE, 2026-07-01 → 07-28** (first measured snapshot — supersedes the hand-pulled figures in the 2026-07-24 note below): **75 clicks / 5,460 impressions / 1.37% CTR / avg position 29.6**. Per term: `property management bronx` **3.7**, `bronx apartment management companies` **1.2**, `property management in the bronx` **3.5** — all already on page 1 — while `bronx property management` sits at **19.9** and `bronx property management companies` at **12.1**. **The earlier "position ~12" figure for `bronx property management` was optimistic; measured, it is ~20.** Not in the top 500 at all: `bronx property manager`, `property management company bronx`, `nyc property management`, `flat fee property management nyc`.
  - **The interesting read:** the short, high-intent variants already rank well and convert (`property management bronx` → 4 clicks from 24 impressions), while the highest-impression terms (`bronx property management companies`, 117 impressions) sit at 12–20 and earn almost nothing. The gap between those two groups is where the clicks are.
  - **⚠️ THE `/areas/` NEIGHBORHOOD PAGES WERE FULLY BUILT AND THEN DROPPED AT OWNER REQUEST. Do not rebuild them from these screenshots without asking.** The work existed — a `build-areas.js` generator, 12 hand-written fact sheets, a filesystem-derived sitemap helper, a dispatch-only workflow — and was removed from the branch before any of it merged. `index.html` was never touched at any point. If it is ever revived, the two findings worth keeping are: **(1) the "80–800 pages" figure is the part that would actually get the site penalised** — checked against Google's doorway-page and scaled-content-abuse policies, where volume alone is not the violation and AI-written prose is explicitly *not* a violation, but thin undifferentiated value is; and **(2) the model must write prose and never facts**, with every figure traced to a hand-written source, because unsourced numbers on a page carrying the real NAP are a liability for a business that separates itself from licensed brokerage activity (`disclaimer.html:60`).
  - **Still true and still unfixed:** `index.html` declares `areaServed` for Fordham, Mott Haven, Pelham Bay, Riverdale and Concourse with **no pages behind those claims** (`index.html:79-83` and again `:113-117`). That was the gap the dropped work addressed. It is harmless but it is an unsupported claim.

- **Homepage title retargeted at the Bronx queries — and a 4-week measurement window is now running** (2026-08-02, PR #276 merged). The first rank snapshot showed the title leading with **NYC** while `nyc property management` earns **zero impressions** (not in the top 500), and every term that does earn impressions is a Bronx one — with the three largest parked on page 2: `bronx property management companies` (117 impr, pos 12.1), `property management companies bronx ny` (103, pos 14.0), `bronx property management` (91, pos 19.9). Neither "Bronx" nor "companies" was in the title.
  - **Four lines changed, all in `<head>`:** `<title>` → `Bronx Property Management Company | DoryAngel Since 2010` (56 chars), the meta description, `og:title` and `twitter:title`. **Nothing visible on the page changed** — verified by hashing `<body>` (byte-identical) plus the hero markup, hero CSS, `heroLines`, `heroUnderlineColors` and the underline SVG separately.
  - **⚠️ The $99 was deliberately kept OUT of the title** (owner's call): price-first invites price shoppers and is the one claim a competitor can undercut. "Since 2010" took the space instead. The flat fee stays in the description, not leading. **Do not "improve" the title by putting the price back.**
  - **⚠️ DO NOT STACK MORE SEO CHANGES BEFORE ~2026-08-30.** The whole point of the rank tracking is attribution; changing several things at once makes the result unreadable. **Decision point:** if `bronx property management` has not moved off ~20 by then, the title was not the constraint — go to backlinks / Google Business Profile rather than iterating on wording.
  - **A screenshot-diff check was attempted and DISCARDED as unusable** — a control rendering the *identical* file twice produced 0.45% differing pixels, the same magnitude as the before/after run. Font fallback and antialiasing are nondeterministic in the CCR sandbox, so that method cannot prove visual equivalence here. **Use structural hashing (body markup, per-component) instead; don't rebuild the screenshot harness.**

- **📋 Read-only audit, 2026-08-02 — findings the owner deliberately DEFERRED. All still open; none are on fire.**
  - **`doryangel.com/tools` 404s and is a live landmine.** `scripts/generate-post.js:411` tells the model to write "free at doryangel.com/tools" into post bodies, but there is **no `tools/index.html` and no `vercel.json` rewrite**. No post has emitted it yet — verified across all 56 posts — so it is harmless *today* and broken the day one does. Cheapest fix is a `/tools` → `/#tools` redirect in `vercel.json`.
  - **One truncated meta description in production:** `why-your-bronx-rentals-insurance-premium-could-jump-25-this` ends mid-word — `"…and how to fight ba"`. The only one of 56; the model hard-clipped to the 155 limit instead of composing to fit. Renders verbatim in Google.
  - **The old github.io Compliance Calendar link is in a 2026-07-30 post** (`content/blog/posts-index.json:66`) — published *after* the generator was repointed on-site, so it sends backlink credit to the personal subdomain the migration existed to retire.
  - **`/tools/pl-dashboard/` is still absent from the sitemap** — and it is the **paid** ($29.99) tool. Must be fixed in **both** `buildSitemap()` (`scripts/build-blog.js:684`) and `sitemap.xml`, or the next Nave run reverts a file-only edit.
  - **✅ Verified healthy, so nobody re-audits these:** **0 of 56** posts exceed the 60-char `seoTitle` or 155-char `seoDescription` limits, none are missing either field, and all 56 end `| DoryAngel` — structurally guaranteed because the suffix is appended by code, not written by the model. Link and asset graph clean: zero broken local hrefs, zero dangling `#` anchors, zero missing images, no duplicate or conflicting JSON-LD, and the 5.0★/21-reviews figure is in sync across all four places it appears.

### Older changes (as of 2026-07-29)

- **⚠️ THE NEXT 10 POSTS ARE ALL MAINTENANCE — this is deliberate, do not "fix" the lack of category variety** (2026-07-29, owner request, branch `claude/regenerate-nav-agent-post-7aos4b`). A new **`content/blog/category-plan.json`** holds a standing multi-run instruction: `{ forceCategory: "diy-property-management", remaining: 10 }`. Nave reads it in `getForcedCategory()` and decrements `remaining` by one **per published post**; at 0 it stops forcing and normal category weighting resumes on its own. Expect a run of consecutive Maintenance & Repairs posts through roughly **2026-08-18** at the 2-day cadence.
  - **To end it early:** set `remaining` to 0 or delete the file. **To extend it:** raise the number. **For a one-off exception:** the workflow's `category` input (`FORCE_CATEGORY`) still wins over the file for that single run.
  - **⚠️ The workflow MUST keep `content/blog/category-plan.json` in its `git add` line.** The decrement happens on the runner; if the file isn't committed it is thrown away with the container, `remaining` never drops, and the plan runs forever. That one line is the whole mechanism.
  - **The decrement is deliberately placed after the index write**, so a run that dies mid-generation doesn't silently burn a slot. Every read/write is wrapped — a missing or malformed plan file logs and is ignored, it never blocks a publish (same fail-open philosophy as the dedupe gate).
  - **Known risk, mitigated but worth watching:** 10 consecutive posts in one category is well outside the documented ~1-in-3 maintenance target, and the semantic dedupe gate only retries twice before failing open. The forced-maintenance prompt now carries an explicit **SUBJECT VARIETY** rule telling the picker to rotate across the whole building (heating, plumbing, roof/facade, electrical, windows, floors, pests, water heaters, basements, trade-hiring and documentation) rather than returning to roofs and boilers. **Skim the titles every few posts** — if they start converging, cut `remaining` short.
  - **📋 SKIM DONE 2026-08-02 — subjects rotated, FORMAT collapsed. Owner chose to let the last 4 run anyway; do not "fix" this retroactively.**
    - **The SUBJECT VARIETY rule worked.** Eight consecutive maintenance posts covered eight distinct systems — plumbing, roof, windows, basement/drainage, electrical, HVAC, pest, AC. **Boilers never reappeared**, which was the specific fear.
    - **But the title template collapsed:** six of the last eight open `"Your Bronx Building's …"`. Nothing in the prompt constrains *sentence shape*, only category and subject. **If this run is ever extended or repeated, that is the rule to add.**
    - **And the `vs. <city>` comparison angle came back three times** (Mount Vernon ×2, Queens ×1) — the same angle a post was deleted over on 2026-07-29.
    - **A duplicate shipped:** `Bronx vs Mt. Vernon HVAC: Who Pays Less?` (07-30) and `Bronx vs Queens AC Repair Costs` (08-01) are the same subject two days apart, competing for the same queries. **The semantic dedupe gate should have caught it** — `findSimilarPublishedTopic()` in `scripts/generate-post.js:286-315` **fails open on error (`:312`), so a silent Haiku failure is indistinguishable from a pass.** Left live at owner's request.
    - **Cadence correction:** the run ends **~2026-08-05, not ~08-18**. Four posts published on **2026-07-30** — an even day the cron (`blog-autopublish.yml:7`, odd days only) never fires on — so they were manual dispatches that burned four slots in one day. `remaining` was 4 as of 08-02.

- **Nave can be pinned to a category, and branch runs no longer go outbound** (2026-07-29, branch `claude/regenerate-nav-agent-post-7aos4b`): the 2026-07-29 auto-publish produced *another* property-management comparison ("Bronx vs. Yonkers… Summer Tenant Turnover") when the intended mix is roughly **1 in 3 posts on maintenance**. Owner asked for it to be regenerated as a maintenance post. There was no way to steer a run — the picker's HARD VARIETY RULE only blocks the same category **twice in a row**, so after a maintenance post on 07-27 it was free to go back to property-management.
  - **New `FORCE_CATEGORY` env / `category` workflow_dispatch input** (`scripts/generate-post.js` + `blog-autopublish.yml`). It overrides the category weighting *and* the variety rule, and is enforced in **three** places — the picker prompt, the parsed result, and the fallback round-robin — so it still holds when the Haiku topic call fails (verified: with an invalid key the fallback picked a `diy-property-management` topic). An unknown value logs a warning and is ignored rather than erroring.
  - **⚠️ `SKIP_BROADCAST` — the reason a branch run is safe.** The workflow sets it whenever `github.ref_name != 'main'`. Nave then writes the post to the index and emails the owner, but sends **no subscriber digest** and writes **no social queue**; Vera's step is skipped by the same condition. Without it a branch run would blast every subscriber a link to a post that only goes live after the merge — a guaranteed 404. **Do not "simplify" this away**, and do not run a branch dispatch with it disabled.
  - **The commit step now pushes to `HEAD:$GITHUB_REF_NAME`, not a hardcoded `main`.** That is what makes a branch dispatch land the post on the branch for review. It also *had* to change: `git push origin main` fails outright off main, because actions/checkout only creates the dispatched branch and `git pull --rebase` never makes a local `main`. The cron and a normal dispatch both run on main, so their behaviour is identical to before.
  - **The 2026-07-29 Bronx-vs-Yonkers post was removed** (index entry, page, hub, sitemap; 50 → 49 posts) and a maintenance post generated in its place. **Its digest blast had already gone out on 07-29**, so the old slug is redirected in `vercel.json` rather than left to 404 — same pattern as the earlier duplicate-post consolidations.
  - **Replacement post:** *"What Should a Bronx Landlord Actually Check on the Roof Before Fall Hits?"* — a genuine building-care walkthrough (built-up vs. modified-bitumen vs. EPDM explained in plain English, $150–$300 inspection cost, what's owner-doable vs. licensed-roofer work). 1,241 words, marginally over the 800–1,200 target.
  - **⚠️ Hero images were picked at RANDOM from the category's query list, ignoring the post's subject** — found because this roof post was illustrated from the query `"radiator heating apartment window"` while its `heroImageAlt` described a tar-and-gravel roof. **Alt text contradicting its own image is worse than no alt text.** New shared `pickImageQuery()` in `scripts/lib/post-utils.js` scores each of the category's queries by word overlap with the title and falls back to the old random pick when nothing matches. Only the *selection* is shared — the per-script `IMAGE_QUERIES` lists stay local, as that module's header requires. `fix-single-image.js`'s `diy-property-management` list had also **drifted out of sync** (missing the roof and boiler queries), so re-rolling a roof post's image there could never have produced a roof photo; it is now in step with `generate-post.js`.
  - **`fix-single-image.yml` is also branch-aware now** (same `HEAD:$GITHUB_REF_NAME` push as the autopublish workflow) — that is how this post's image was corrected on the branch.
  - **Still true:** the CCR sandbox has no `ANTHROPIC_API_KEY`, so Nave cannot be run locally. Use `workflow_dispatch` (optionally with `dry_run: true`) to exercise it. The sandbox also cannot reach `images.unsplash.com`, so a hero image can't be eyeballed in-session — check it on the PR preview.

### Older changes (as of 2026-07-28)

- **Vera now posts to Facebook via Make, not the Graph API — and the silent skip is fixed** (2026-07-28, branch `claude/heck-vera-ahent-k1gew6`, PR #267 merged + follow-up): an audit found that **every auto-publish run from 2026-06-20 through 2026-07-27 skipped Facebook entirely**, on a green checkmark. `FACEBOOK_PAGE_ACCESS_TOKEN` was empty, so `postToFacebook()` hit its guard clause and `console.log`-ed a skip. Over a month of every-2-day publishing produced zero Facebook posts and nothing surfaced it. PR #267 fixed the silence; the follow-up removed the token requirement altogether.
  - **⚠️ FOUR INVARIANTS — breaking any of these silently regresses Vera. Read before touching it or the publish workflow.**
    1. **Vera must NEVER fail the workflow.** Every exit path is `exit 0` and the queue read is inside the try/catch. Previously `JSON.parse` sat *outside* it, so a truncated `/tmp/social-queue.json` would have thrown unhandled → node exits non-zero → **the workflow dies before the commit step and the post never publishes at all**. The `resend` import is also **dynamic, inside `emailFallback()`** — a top-level import throws at module load, before any try/catch can catch it.
    2. **Vera runs AFTER the commit/push step.** It was step 7 of 8; it is now last. It also polls the post URL (6 × 15s, then posts anyway) so Facebook isn't handed a link that 404s while Vercel redeploys. **Do not move it back above the commit** while "tidying" the step order.
    3. **A 200 from the Make webhook is NOT proof of a post.** Make replies `Accepted` instantly unless the scenario ends in a **Webhook Response** module, so Vera requires a `postId`/`id` in the reply and treats its absence as a failure. Deleting that response module re-creates the exact silent-success bug this whole episode was about. Verified locally against a stub: `Accepted` → `::error::`, not success.
    4. **Nave's approval email is INSTAGRAM-ONLY.** STEP 3 used to say "Facebook AND Instagram". With Vera posting, following that email double-posts to Facebook. If a Facebook post fails, Vera sends a **separate** "Facebook auto-post failed" email carrying the caption + link. **Do not re-add Facebook to Nave's STEP 3.**
  - **Why Make instead of the Graph API.** Direct posting needs a hand-minted **long-lived Page token**, and getting one means a four-step exchange through the Graph API Explorer — hostile enough that the first attempt leaked a user token by pasting it into the Explorer's *path* field instead of its *token* field. Make already runs nine scenarios for this business and has a native `facebook-pages` app (v6, **Create a Post**) whose connection is **OAuth** — Make holds and refreshes it. **No token to mint, none to leak, none to expire.** Owner effort dropped from a 4-step exchange to one "Connect" click. **Do not "simplify" this back into a direct `graph.facebook.com` call.**
  - **The wiring.** Vera POSTs `{secret, slug, message, link}` to the pre-existing, previously-unused webhook **"DoryAngel Blog Publish Hook"** (id 2871253, `hook.eu1.make.com/ura1w6nxwhdgbkg0ebbdu6fwiuq4zi38`) → Make scenario **"DoryAngel Blog — Facebook Auto-Post"**: Webhook → Filter (shared secret) → Facebook Pages Create a Post → Webhook Response (returns the post id). **The filter is not optional** — without it the public hook URL lets anyone make the DoryAngel Page publish arbitrary text. It reuses **`DIGEST_WEBHOOK_SECRET`** rather than a new secret, deliberately: it is already set and already wired through `blog-autopublish.yml`, so this added **zero new secrets** for the owner. One secret covering two purposes is the accepted tradeoff.
  - **`FACEBOOK_PAGE_ID` and `FACEBOOK_PAGE_ACCESS_TOKEN` are now UNUSED** — nothing reads them. Safe to delete from repo secrets. `.github/workflows/vera-verify.yml` (added in PR #267 to check the Graph token) was deleted for the same reason.
  - **✅ CONFIRMED LIVE 2026-07-30** — first real auto-post: `[Vera] Posted to Facebook — post ID: 208710759294686_1353193953652886`, Make execution `operations: 3` (webhook → Facebook → response). Scenario **6735249**, connection **9415750** ("DoryAngel Facebook Page", OAuth, shown expiring 2026-09-28 — Make refreshes it; if it ever lapses the owner gets an email and reconnects in one click). Page is **"Doryangel Asset management"** (`208710759294686`).
  - **⚠️ TWO GOTCHAS THAT COST HOURS — read before debugging this scenario again.**
    1. **A brand-new Make webhook has NO field definitions, so `{{1.secret}}` silently resolves to EMPTY and every filter comparison fails.** The scenario looks perfect and the execution reports SUCCESS with `operations: 1`. Fix = attach a data structure to the hook. **The Make API cannot do this** — `hooks_update` accepts `datastructureId` / `dataStructureId` and silently discards both; the stored field is **`udt`** and it only got set through the UI (webhook module → the hook's **Edit** dialog → Data structure). Data structure 509086 "DoryAngel Blog Publish payload" is attached to hook 2871253 now — don't detach it.
    2. **Never transcribe the shared secret from a screenshot.** It contains both `I` (capital i) and `l` (lowercase L); reading it off an image swapped them, the length still matched at 32 chars, and the filter failed silently. Read it programmatically from scenario 6347243's filter instead, or copy-paste it — never retype.
  - **Debugging trick that found it:** temporarily reorder the flow to `Webhook → Webhook Response → Facebook` and have the response body return `{{length(1.secret)}}` / `{{length(1.message)}}` / `{{1.link}}`. Vera logs the response verbatim in its `::error::` annotation, so one workflow run reveals whether the fields resolve — no Make UI access needed. Restore the original order afterwards or the response won't carry the post id.
  - **Also from PR #267, still in place:** skips/failures are GitHub Actions annotations (`::warning::` unconfigured, `::error::` failed) — a `console.log` is what hid this for a month, don't downgrade them; and `readJson()` wraps the response parse so a non-JSON answer reports its status and body instead of `Unexpected token`.
- **Offer renamed to "Free Rental Analysis" + a compliance pass** (2026-07-27, branch `claude/hero-rotating-sentences-myjdhz`, PR #264 merged): started as an A/B test of the CTA wording and ended up as an offer alignment plus a set of governance fixes. A governance/compliance subagent reviewed the change before merge.
  - **⚠️ FIVE THINGS A FUTURE SESSION COULD UNDO WITHOUT KNOWING WHY. Read before touching the contact form, the CTA, or Make.**
    1. **Do NOT re-propose the CTA A/B test.** It was evaluated and rejected on arithmetic, not taste: GSC shows **~63 organic clicks / 28 days**. At a generous ~400 sessions/month split two ways, detecting a realistic 20% lift needs **~12,900 sessions per arm — about 5 years**. A split would only halve already-thin traffic. Revisit only if traffic grows an order of magnitude.
    2. **The TCPA checkbox must stay OPTIONAL** (`index.html`, `#tcpa-consent`). It used to be `required`, so nobody could contact DoryAngel without consenting to calls/texts — contradicting `sms-consent.html:58` ("entirely voluntary… not required as a condition of receiving our services") and FCC **47 CFR 64.1200(a)(2)**, which bars conditioning a transaction on marketing consent. Do not re-add `required` while "tidying" the form.
    3. **⚠️ FAQ copy exists TWICE on `faq/index.html` and `guides/bronx-landlord-compliance/index.html` — in the visible HTML and again in the `FAQPage` JSON-LD — and the two are NOT byte-identical.** The JSON-LD is a deliberately normalised variant: ASCII punctuation (`-` not `—`, straight quotes), expanded contractions ("we are" not "we're"), and occasionally slightly different wording. **Consequence: a find-and-replace will usually hit only ONE of the two copies and silently desync them** — that happened during the 2026-07-27 CTA rename, where the visible copy wrapped the phrase in an `<a>` tag and the JSON-LD did not. Google needs the structured data to reflect what's on the page. **When editing FAQ wording, change both copies as a pair and eyeball the result — do not trust a bulk replace.** (A verbatim checker was prototyped and deliberately **not** shipped: because the two copies legitimately differ, any strict comparison either floods false alarms or, if loosened enough to pass, stops catching the real desync. It gave false confidence both ways.)
    4. **Any Make scenario whose `lastEdit` predates 2026-07-07 may still contain `beta.doryangel.com`.** The post-cutover repoint only ever covered 5549170 / 6347243 / 6346876 — the note claiming templates were done was incomplete. **6335620** (×2 links) and **6335621** (×1) were fixed on 2026-07-27. **Still unchecked: `5546593` and `5546729`** (compliance calendar, both pre-cutover but 0 executions) and the Feb legacy pair **`4406127` / `4482886`**.
    5. **Never copy the Turnstile secret (in 6335620's blueprint) or the broadcast shared secret (in 6347243's filter) into this repo, a commit, or a PR body.** They are server-side by design.
  - **The CTA is now "Free Rental Analysis"** everywhere — buttons, nav, mobile drawer, announce bar across `index.html`, `faq/`, `guides/bronx-landlord-compliance/`, `tax-checklist/` (×2), `flat-fee-vs-commission/`, `tools/pl-dashboard/`, plus `scripts/build-blog.js` and all 48 regenerated blog pages (verified: the next auto-publish inherited it, 49/49 posts now correct). The contact heading previously said **"Book your free consultation today"** while every button said "Free Property Audit" and the form was a general contact form — **three different promises for one click**. The heading now matches.
  - **Why the offer is legally safe — do not reframe it.** Owner confirmed the real division: **DoryAngel produces the rent estimate as property managers; the licensed broker partners are involved only if the owner then decides to list.** That is exactly what `disclaimer.html:60` and `index.html:4938` already say ("activities requiring a real estate brokerage licence are handled exclusively by our licensed broker partners"). Describe the output as a **market opinion**, never an appraisal, valuation, or broker price opinion. An earlier draft was halted over this and the framing above is what cleared it.
  - **Deliberately NOT done:** no new landing page, no new form fields, no webhook payload changes. A `free-rental-analysis/` landing page was built and then reverted at owner request — the offer is communicated through the existing CTA and contact form instead. Keep it that way unless asked.
  - **Also fixed:** `flat-fee-vs-commission` lead form had **four inputs with no `<label>` and no `id`** (screen readers announced "edit text, blank" — WCAG 1.3.1/3.3.2/4.1.2); the "free, **exact** estimate" overclaim was removed; and the form focus ring (`rgba(58,123,221,0.12)` — 12% alpha in a stale pre-rebrand blue) now uses `--blue` at a visible strength plus a `:focus-visible` outline (WCAG 1.4.11).
  - **Known and NOT fixed — Turnstile protects the pipeline but not the inbox.** In `handleFormSubmit()` the full FormData goes to Web3Forms (which emails the owner) *before* the Make webhook fires the server-side Turnstile check, so bot submissions still reach the inbox. **The fix is one line but is ORDER-CRITICAL: the owner must first add the Turnstile secret key to the Web3Forms dashboard, and only then may `data.delete('cf-turnstile-response')` be removed.** Removing it first breaks the contact form outright (`success:false` → "Error – please call") — that exact regression shipped on 2026-06-28 and is why the line exists. A `_honey` honeypot is the interim protection.
    - **⚠️ CORRECTION 2026-08-02 — it is THREE places, not one, and the gap is worse than described above.** `data.delete('cf-turnstile-response')` appears at **`index.html:5093` (contact), `:6030` (tool gate) and `:6118` (third form)**. A fix is a three-site change and the order-critical hazard applies at each. And in `handleFormSubmit()` the Make/Turnstile call (`:5105`) sits **inside `if (result.success)`** — so the owner-notification email is not merely sent first, it is a **precondition for the Turnstile check running at all**. A bot that never solves the challenge still gets `result.success === true` and still reaches the inbox. **Correction to the correction: it is TWO `data.delete` calls, not three** — `:5093` and `:6030`. The third Web3Forms call (`:6118`, notify gate) has no delete and its form has no Turnstile widget, so there is nothing to change there.
    - **🛑 DO NOT PERFORM THE FIX AS DESCRIBED ABOVE. A design review on 2026-08-02 found it ships a WORSE bug than the one it fixes.** Three independent blockers, each verified:
      1. **Turnstile tokens are SINGLE-USE.** Removing the delete means Web3Forms redeems the token first; Make scenario 6335620's own `siteverify` then receives an already-redeemed token, gets `timeout-or-duplicate`, and its filter silently blocks the Gmail module. **Result: every visitor stops receiving their confirmation email, 100% of the time, with no error anywhere.** You cannot have two independent validators on one token — **pick one gate**.
      2. **Turnstile on Web3Forms is a PRO feature** (~$12/mo). The instruction above reads like a free dashboard toggle. It is not.
      3. **The access key `6991fa6b…` is shared by 8 forms across 6 files** — `index.html` ×3, `broker-partner.html` ×2, `flat-fee-vs-commission/`, `guides/bronx-landlord-compliance/`, `project/`. Web3Forms' captcha setting is **per access key**, so enabling it hits all 8 at once, and **5 of them have no Turnstile widget at all** — they would break instantly. There is no way to canary one form.
    - **⚠️ THE SPAM DOES NOT COME THROUGH THE PAGE, so no client-side change can stop it.** The access key is public in the page source; bots POST straight to `api.web3forms.com/submit` and never load the site. Evidence: scenario 6335620 has **~7 executions in 4 weeks** — if bots used the form, Make would fire on every one. **The only thing that stops inbox spam is rotating the key server-side.**
    - **⚠️ `_honey` IS THE WRONG FIELD AND PROVIDES ZERO PROTECTION.** The claim above that "a `_honey` honeypot is the interim protection" is **false**. `_honey` is FormSubmit.co's convention; **Web3Forms uses `botcheck`** (a hidden checkbox). Only `index.html:4889` has it right. `index.html:2211`, `:2262`, `tax-checklist/index.html:376`, `flat-fee-vs-commission/index.html:596` and `guides/bronx-landlord-compliance/index.html:652` all use `_honey`, which Web3Forms treats as an ordinary data field and mails to the owner.
    - **⚠️ WEB3FORMS IS THE *ONLY* OWNER NOTIFICATION FOR CONTACT LEADS — do not remove it without a replacement.** Scenario 6335620 is named "Confirmation Email" and contains exactly three modules: webhook → siteverify → **Gmail to `{{1.email}}`, the VISITOR**. There is **no owner-notify module and no sheet**. The note at the 2026-06-28 entry claiming "the downstream email/sheet modules are gated" is half wrong for this scenario.
    - **The recommended path instead, in this order, each step individually safe:** (1) **rotate the Web3Forms access key and mint a separate key per form** — kills current direct-POST spam, dissolves the 8-form blast radius, costs nothing, no ordering constraint; (2) **additively** add an owner-notify Gmail module + sheet row to 6335620 after its existing filter, so the owner briefly gets two alerts per lead (a loud, safe failure mode); (3) confirm duplicates arrive on real leads; (4) **only then** remove the Web3Forms POST from `handleFormSubmit`. At no point is the owner receiving zero notifications.
    - **Do NOT delete the old access key before the new keys are deployed** — that breaks all 8 forms instantly.
  - **Digest unsubscribe checked and is fine:** 6347243's unsubscribe is a working `mailto:` link, not the orphaned `a3mb6f18…` webhook — CAN-SPAM-compliant, just manual. Three hooks have no scenario attached (Blog Publish, Digest Post Published, Digest Unsubscribe) and nothing in the repo calls them; safe to delete for tidiness.

- **Hero rework — anchored rotating promise, Google rating above the fold, desktop portal split** (2026-07-27, branch `claude/hero-rotating-sentences-myjdhz`, PRs #261 + #262 both merged to main): owner said the hero "felt like it was missing charisma and attraction" and asked for the rotating sentences to be reworked. The plan was then re-checked against how successful property-management sites are built, which **changed the priorities** — the rotating headline turned out not to be the main lever.
  - **⚠️ THREE INVARIANTS — breaking any of these silently regresses the hero. Read before touching it.**
    1. **Hero slogans must fit ONE line at 320px.** `.hero-mask` has a **fixed height** (`1.26em`) so the hero can't shift mid-rotation, and `.hero-rotator` is `white-space: nowrap`. A longer slogan can't wrap — it gets clipped instead. Measured worst case has ~13% margin at 320px. **Never add a slogan without re-measuring.**
    2. **Do NOT put `.reveal` back on the hero `<h1>`.** `.reveal` starts at `opacity:0`, which makes the element ineligible for LCP during the critical window; the rotation then re-registers it as a *late* candidate. Measured **LCP 6.7s** with it, **~0.3s** without. The old `heroFlagWave` animation had been accidentally masking this.
    3. **The sticky call button must stay well above the bottom strip** (`bottom: 111px`). The Retell widget renders its "Need support?" bar across the **bottom-RIGHT**, even though the config says `data-chat-bubble-position: 'left'`. Moving the button to `bottom:24px` buried it behind that bar — that regression shipped in #261 and was fixed in #262. **Trust the screenshot, not the config value.**
  - **Rotator restructured**: only the **promise line** rotates now, against a fixed two-line anchor ("Your Property, Cared For / Like It's Our Own."), so the indexed H1 text stays stable for SEO. The whole-block fade became a **mask reveal** (new line rises in as the old exits) plus a **hand-drawn SVG underline** that redraws each swap via `stroke-dashoffset`. **`heroFlagWave` deleted** — it was a permanent rotate+skew on the H1 that read as unstable and kept the serif from ever rasterizing crisply. Interval 9s → **6s**. Markup is `.hero-promise > .hero-mask > .hero-rotator` + a sibling `.hero-underline` (the SVG sits *outside* the mask so `overflow:hidden` can't clip it).
  - **Underline is bolder and changes colour per slogan** (added 2026-07-27, same branch, owner request): `stroke-width` 3.5 → **5.25** (~1.5×; the line renders far thinner than the authored value because `preserveAspectRatio="none"` squashes the stroke by `svgHeight/12` — it is only ~1.3px at 320px at the old value). **Thicken via `stroke-width` only** — growing `.hero-underline`'s `height` also moves the path, which sits at y=8.5 of a 12-unit viewBox, and drags it into descenders. Colour comes from a `--hero-underline-color` custom property set by `setUnderlineColor()`, paired by index with `heroUnderlineColors` = brand blue `#1A5BB5` / amber `#F59E0B` / emerald `#0E9F6E` / teal `#0891B2` / violet `#7C3AED` / brand navy `#0D1E3A`. **`heroUnderlineColors` must stay in step with `heroLines`** (indexed with `%` so a mismatch can't throw). Opens and closes on brand colours deliberately, so the cycle reads as anchored rather than a rainbow; index 0 is also the first-paint and reduced-motion state, which is why it is brand blue. The SVG is `aria-hidden` and decorative, so no WCAG contrast minimum applies.
  - **6 slogans, all benefit-carrying** (`heroLines` in the inline JS): research found benefit-focused headlines convert ~31% better than brand-focused, and rotating headlines are *not* themselves a proven win — headline quality dominates format. So every rotation state has to stand alone. Current set: `Real Peace of Mind.` (index 0, matches the markup) / `Repairs, Handled.` / `Every Dollar Visible.` / `Nothing Hidden.` / `We Always Answer.` / `Licensed & Insured.` **No pricing in the hero — owner's explicit call**; the $99 rate stays in the pricing section and FAQ.
  - **Google rating pulled above the fold**: the **5.0 ★ / 21 reviews** badge now sits between the CTAs and the stats row, reusing the testimonials badge markup with a new `.google-rating-badge--hero` light-theme modifier. It had been fully built and styled but buried thousands of lines down; industry guidance is explicit that review badges belong in the hero. **The 5.0 / 21 figures now live in THREE places that must stay in sync**: the `aggregateRating` JSON-LD in `<head>`, the testimonials section, and this hero badge. (This also closes the long-standing "social proof counters above the fold" roadmap item.)
  - **Desktop split at ≥1024px** (below that the hero is the unchanged centred column): `.hero-visual` — the card stack that had been fully styled but `display:none` — is un-hidden and now shows the **real owner-portal screenshot** (`assets/portal-live-dashboard`, 26KB WebP + JPG fallback) instead of the old fabricated dashboard card, which carried invented figures and a stale "Lease expires Oct 2025". The `.hero-card-bg` "Portfolio Health / 98%" card floats over its lower-left corner. **`.hero-visual` must be `flex-direction: column`** — the disclaimer note is a sibling of the card stack and otherwise shares the column width, squeezing the shot to ~250px. Frame is cropped to `aspect-ratio: 676/470` because the source image's bottom ~120px is the portal micro-site's **own carousel caption** ("01 / Live Dashboard / At a glance") — wanted in `#about`, just chrome in the hero. Same asset, no second file.
  - **`2 hr / Avg. response time`** replaces the vague "24/7 / Support & response" stat (owner confirmed 2-hour response is real). Competitors win on specifics here. 24/7 is not lost — it moved to the trust-bar rotator as the emergency-maintenance line. "2 hr" also still works with the existing count-up, which only animates values starting with a digit.
  - **Mobile fixes**: `.hero-eyebrow` was `display:none` below 900px, hiding the positioning line from the *majority* of traffic — it now renders, trimmed to its geo half via `.hero-eyebrow-tail`. CTAs raised 44px → **48px** min-height.
  - **Sticky call button no longer covers the hero stats**: it now stays hidden until the hero scrolls past (IntersectionObserver on `.hero-wrap`, toggling `.is-revealed`), because repositioning alone can't fix it — the stats land at a different height on every device. It is `visibility:hidden` while hidden, so it takes no pointer events. **Gotcha:** that init is deferred to `DOMContentLoaded` — the inline script runs while the document is still parsing and the button markup sits near `</body>`, so an inline `querySelector` returns `null` and the observer is never attached, with no error.
  - **Also**: `.hero-photo.active` opacity **0.22 → 0.10** (at 22% the photos were a grey wash fighting the copy for contrast), and hero text column widened with the H1 rescaled to `clamp(36px, 3.9vw, 50px)` in the split (the anchor line needs ~641px at 58px and overflowed the narrower column).
  - **Verified** in headless Chromium at 320–1920px: H1 height identical across all six slogans, no slogan can wrap, alignment exact (every element centres on phone; every left-column element shares one left edge on desktop), reduced-motion pins to line one, **LCP ~0.3s single candidate, CLS 0.0000**. **Not verified: DM Serif Display's real metrics** — the CCR sandbox cannot load Google Fonts, so all text measurements used the fallback serif. `nowrap` makes the no-clip guarantee font-independent, but confirm on a real device if slogans change.

- **Nave: maintenance content direction + short social captions** (2026-07-26, branch `claude/nave-agent-u9rhbw`, PR #259 merged to main): owner asked to steer the blog toward down-to-earth, plainly-explained property maintenance, and to cut the Facebook/Instagram caption to a recommended post size. A review of Nave first found the real problem: **maintenance was already 12 of 48 post titles, but always framed as a cost** ("How Much Are Deferred Repairs Costing You…"), never as instruction — because three separate mechanisms pushed every draft back into the cost frame.
  - **Category repointed — `diy-property-management` now means "Maintenance & Repairs".** It had become a de-facto compliance bucket: **4 of its 5 posts were already claimed by the compliance pillar hub**. The 4 compliance posts + a vacancy post moved out to `property-management`; **6 genuine building-care posts moved in** (spring exterior inspection, spring maintenance checklist, deferred repairs, July AC breakdown, summer pest control, Queens spring turnover). New mix: PM 21 / maintenance 6 / investments 9 / automation 7 / broker 5. **The slug is deliberately unchanged** — see the ⚠️ note under "Blog architecture" for the three label sites that must stay in sync. Hub up-links survived the move because `GUIDE_HUBS` matches by slug, not category.
  - **Fixed the three mechanisms that re-cost-framed every draft**: `SYSTEM_PROMPT` rule 1 now allows a practical how-to title without a bolted-on dollar figure; rule 2 lets a how-to excerpt promise a capability instead of a pain; `researchTopic()` asks for **trade facts** (vendor rates, part costs, failure modes, permit/licensed-trade requirements, heat-season timing) when the category is maintenance, instead of the fines-and-court-timeline facts that suit compliance; and the editor's `NYC_SPECIFICITY` check now accepts a concrete trade specific, so a good how-to citing real repair costs no longer FAILs for lacking a statute.
  - **New `SYSTEM_PROMPT` rule 16 — DOWN-TO-EARTH MAINTENANCE**: teach the thing in plain English, gloss every trade term the first time, give checkable Bronx costs/timing, say plainly which jobs need a licensed trade or DOB permit, and weave one genuine building-care detail into non-maintenance posts too. Rule 7 gained two maintenance shapes (physical-order building walkthrough; symptom-first "the top-floor radiator is cold" → cause → fix).
  - **Facebook/Instagram caption cut from 200–280 words to 40–80** (never >100), with a **one-sentence hook under 125 chars** (all that shows before "See more" on both platforms) and **exactly ONE tool link instead of all five** — the old 5-link block ran ~500 chars and sat entirely below the fold, where it read as spam and suppressed reach. Broker posts always use the Broker Program line; maintenance posts default to the Compliance Calendar (which is literally the preventive-maintenance-schedule tool). Nave now logs caption word/char/hook length each run so a regression is visible in the Actions log.
  - **Also fixed the category-rotation drift** found in the same review: the "avoid repeating the same category" line was a soft preference losing to "when in doubt pick property-management", which had produced **five straight property-management posts** (11 of the last 12 non-maintenance). It is now a hard rule, plus a target of roughly 1 in 3 posts being maintenance. `FALLBACK_TOPICS` rebalanced to 31 entries (PM 15 / maintenance 7 / investments 5 / automation 2 / broker 2) with 7 new plain-English maintenance topics; also corrected a pre-existing miscategorization (the Bronx-vs-New-Rochelle entry sat in the investments block but carried the `diy` category).
  - **Verified live by dry run before merge** (workflow_dispatch on the branch with `dry_run: true` — run 30195123304; the CCR sandbox has no `ANTHROPIC_API_KEY`, so dispatching the workflow is the only way to test Nave from a session). Nave picked a **maintenance** topic on the first try and produced a genuine how-to title with no cost framing — *"What Should a Bronx Landlord Actually Check on the Roof After Summer Heat, Before Fall Rain?"* The body did exactly what rule 16 asks: explained EPDM/TPO membranes in plain English, listed the four failure modes, split owner-doable tasks (clearing drains, photographing ponding) from DOB-licensed-only work, and quoted real Bronx numbers ($300–$500 inspection, $85–$120/hr labor, $12k–$35k replacement). Caption came out **82 words / 596 chars, one tool link, 5 hashtags**. The editor caught a real `QUESTION_HEADINGS` failure and the rewrite fixed it. **To re-test after any Nave prompt change, dispatch `blog-autopublish.yml` with `dry_run: true`** — it sends the owner the preview email and writes/commits/broadcasts nothing.
  - **Three things left open from that run, worth watching rather than fixing now:**
    - **Caption hook came in at 134 chars vs the 125 target** — a soft instruction in the prompt, not an enforced limit, so the tail clips at the "See more" fold. Tighten the wording or add a hard check if it keeps overshooting; `Caption: N words / N chars, hook N chars` is logged every run.
    - **The dedupe gate rejected 2 topics and flagged a 3rd before failing open.** It killed a fall-boiler topic (too close to the July AC post) and a Bronx-vs-Yonkers comparison (too close to Bronx-vs-Mount-Vernon). The final call was right, but **seasonal maintenance topics naturally resemble each other**, so expect this gate to reject more often as the maintenance cluster grows — if it starts blocking genuinely distinct posts, loosen `findSimilarPublishedTopic()` rather than the topic picker.
    - **The widened `NYC_SPECIFICITY` check is not yet proven on a no-statute how-to** — the test post happened to cite both trade costs *and* § 27-2056, so it would have passed under the old rule too.
  - Clarity returned **0 blog-category signals** on this run (14 raw URL rows) and the new diagnostic line said so explicitly — the "thin data post-Vercel-move" branch works as intended.

- **Internal-linking / "topic-cluster" exposure pass — pillar hub + site-wide cross-linking** (2026-07-24, branch `claude/graphify-zxb3qi`, PRs #253–#256 all merged to main): kicked off from the **"DoryAngel Codebase Knowledge Graph"** artifact review, whose clearest finding was that the blog was a pile of **isolated leaves** (96 of 292 nodes degree-1) — good content, but posts barely linked to each other, so Google captured little topical authority. Fixed in four merged PRs:
  - **PR #253 — Compliance pillar hub page** at **`/guides/bronx-landlord-compliance/`** (new top-level `guides/` directory). A real hub: intro + jump-nav, **9 curated compliance posts** grouped into 5 themed sections (filing deadlines, rent regulation & Good Cause, evictions & housing court, legal tenant screening, deposits & taxes), a free-tools callout, a compliance FAQ, and a Web3Forms lead form. Reuses the sibling landing-page shell (nav/drawer/footer + brand CSS). SEO: canonical + OG/Twitter + three JSON-LD blocks (`BreadcrumbList`, `CollectionPage`+`ItemList` of the 9, `FAQPage`). **Gotcha fixed during review:** the in-page jump nav was a `<nav>` element, and the shared shell's global `nav { position: fixed; top: 40px }` rule hijacked it into a second fixed bar overlapping the logo on mobile — switched it to `<div role="navigation">`. Also centered the section headings/subs/group-heads to match the sibling pages (was a centered hero over left-aligned sections). **Lesson for future pages built on this shell: any `<nav>`/`<header>`/`<footer>` you add inside the body inherits that global element-level CSS — use scoped `<div role=...>` for in-page structures.**
  - **PR #254 — Toolkit card + generalized name.** Added the guide as a real **"NYC Compliance Guide"** `tool-card` in the homepage DIY Landlord Toolkit (`#tools`), not just the nav; the free-tools grid went from a row of 3 to a symmetric **2×2** (desktop `repeat(3,1fr)` → `repeat(2,1fr)`) to fit the 4th card cleanly. **Renamed the guide from "Bronx" → "NYC"** across all visible touchpoints (card, nav dropdown, mobile drawer, and the guide page's title/meta/OG/JSON-LD/eyebrow/H1/announce/intro) at owner request, to open it to all five boroughs — accurate since the content is citywide NYC law (Good Cause, rent stabilization, RPIE, HPD/DOB/FDNY). **Bronx kept as the lead example** in the copy to preserve the traffic angle. **The URL slug `/guides/bronx-landlord-compliance/` was deliberately NOT changed** (display name ≠ slug is fine; changing it would break the merged links) — so the folder/slug says "bronx" but the page reads "NYC".
  - **PR #255 — cross-cluster "Continue reading" + hub up-links** (`scripts/build-blog.js`). `getRelatedPosts()` previously linked only 3 same-category posts, siloing the clusters. Now it blends across categories (2 most-relevant in-cluster + ≥1 most-relevant cross-cluster, scored by shared hashtags ×3 + title/excerpt word overlap) so **0 of 47 posts stay siloed**. The 9 compliance-cluster posts also get a prominent **"Read the full NYC Landlord Compliance Guide →"** up-link (first item in their internal-links block) — the slug set lives in `GUIDE_HUBS` in build-blog.js. **Bonus fix:** `buildSitemap()` was omitting the guide URL, so the next Nave run would have silently dropped `/guides/bronx-landlord-compliance/` from `sitemap.xml` — now included. All 47 pages regenerated.
  - **PR #256 — Nave weaves in-body links into new posts** (`scripts/generate-post.js`). New `SYSTEM_PROMPT` rule 15 + `pickLinkablePosts()` (offers Nave the top-8 most-relevant existing posts by title/excerpt overlap + same-category bonus, plus the compliance hub) so each new post gets 2–3 contextual inline links as it's written. **404 guard: `sanitizeInternalLinks()`** unwraps any `/blog/` or `/guides/` link whose slug doesn't exist into plain text (preserving anchor text), so a hallucinated URL can never ship as a 404; homepage `#anchor` and `/tools` links pass through. Applied on both the initial draft and the editor rewrite. Effect first shows on the next scheduled auto-publish.
  - **Parked (owner deprioritized):** the **Comparison** and **DIY** pillar hubs (same pattern) were discussed but the owner deferred them — pick up from the compliance page as the template if wanted.
  - **GSC properties + host consolidation (2026-07-24):** owner's Search Console has a **Domain property `doryangel.com`** (ideal — covers apex + www automatically), redundant `https://www.doryangel.com/` + `http://www.doryangel.com/` URL-prefix properties, and the **old GitHub Pages property `dror75p-ops.github.io/Doryangel-web-3-carousel-blog/`**. There is **no separate `beta.` property**, so the "change of address beta→www" follow-up (#1 below) is **N/A** — the apex→www 308 + `www` canonicals + the Domain property already consolidate everything. **Consolidation CONFIRMED working 2026-07-24:** Google's indexed homepage (the `share.google` result link) resolves to **`https://www.doryangel.com/`**, and `beta.doryangel.com` **redirects** (beta → apex → www) rather than serving a live duplicate. So there is nothing to fix host-side. Remaining owner action is just: **resubmit `sitemap.xml` to the Domain property + Request Indexing** on `/`, `/blog/`, `/guides/bronx-landlord-compliance/`. (The `beta` DNS record still resolves-and-redirects — harmless; delete it someday for tidiness. If ever needed, GitHub Pages for that repo can be turned off at repo → Settings → Pages → Source: None.) **⚠️ SUPERSEDED 2026-08-06 — Pages was never turned off and `dror75p-ops.github.io/Doryangel-web-3-carousel-blog/` returns 200, serving a full live duplicate. See the host-check entry at the top of "Last changes"; read that property's impressions before switching Pages off.**
  - **GSC performance review (2026-07-24, 28-day export 2026-06-24→07-21).** ⚠️ **SUPERSEDED — this was a hand-pulled export and its per-term positions are wrong. Rankings are now measured automatically every day; see the 2026-07-31 entry at the top and `project/seo/rank-history.json` for real figures.** In particular, "bronx property management" is **~20**, not ~12. Retained for the traffic-trend context only: ~**63 clicks / 4,731 impressions / 1.33% CTR**, with **impressions ~2.5× in the back half** (1,343 → 3,388) — climbing on the right **owner-facing** queries ("bronx property management", "nyc property management"). **The ceiling is ranking position, not the titles/content:** median query position ~49, and the money terms ("bronx property management") sit at **position ~12 — top of page 2**, so they earn impressions but almost no clicks. The `http://doryangel.com` (apex) + `beta.doryangel.com` URLs that show as separate pages in the report are **pre-cutover legacy** (the 28-day window straddles the 2026-07-07 Vercel cutover) and will fade as Google recrawls — NOT a live-duplicate problem (see consolidation note above). Interpretation: the internal-linking overhaul (PRs #253–#256) targets exactly this page-2→page-1 problem via topical authority; **the lever now is the GSC reindex + a few weeks of recrawl**, not more code. Re-pull the export in 2–4 weeks and track avg position on "bronx property management" (want single digits) + clicks. Faster accelerators if wanted: backlinks (post the free tools to r/Landlord, BiggerPockets, NYC landlord forums), Google Business Profile, and the still-available Comparison/DIY pillar hubs.

- **Owner-portal preview: heavy hero iframe → lightweight rotating screenshot stripe in `#about`** (2026-07-22, branch `claude/vercel-screenshot-analysis-sz8bpv`, PR #248): the owner flagged (via Vercel screenshots) that the hero felt heavy. Root cause: the `doryangel-owner-portal-reports` preview was a live `<iframe>` embedding an entire external site, and after the 2026-07-19 move into the hero it sat high enough that its 400px IntersectionObserver preload buffer fired almost immediately on mobile — so it loaded a whole second website during the hero's critical/LCP window. **This reverses the 2026-07-19 "portal preview into the hero" placement** (see the entry below, now annotated).
  - **Removed** the iframe embed + all its CSS (`#portal-embed-outer` / `#portal-open-link`) from the hero. Hero is now headline + CTAs + stats only.
  - **Added a static-screenshot showcase to `#about` ("Our Approach"), right after the six pillars** — its natural home (Pillar 01 is "Transparent Reporting") and safely below the fold, so it never touches LCP. It **cross-fades through all 7 real portal screens** (Live Dashboard, Task Tracker, Bills & Vendors, Outstanding Balances, Lease Ledger, Reports Library, Mobile App). New CSS class prefix `.portal-shots` / `.portal-stage` / `.portal-slide` / `.portal-dot`; markup + rotation JS live inline in `#about`.
  - **New assets** `assets/portal-*.{webp,jpg}` (7 pairs, WebP ~25KB each, 177KB total for all 7). These were generated by cloning the `dror75p-ops/doryangel-owner-portal-reports` repo, rendering its `index.html` headless (the live github.io host is unreachable from the CCR sandbox — same network limit noted for Retell/Clarity), forcing the carousel's `overflow:visible` so all panels paint, then cropping each panel and encoding to WebP+JPG via Pillow. If the portal's screens change, re-run that capture to refresh these.
  - **Perf/robustness**: lazy-loaded via IntersectionObserver (300px early — nothing loads until the section nears the viewport); `.portal-stage` reserves height via `aspect-ratio: 676/592` so there's no layout shift; autoplay (3.8s) pauses on hover + when the tab is hidden; honors `prefers-reduced-motion` (no autoplay); clickable dots; `<noscript>` fallback image. Kept the "illustrative sample data — your real portfolio is private and protected" disclaimer and a link to `doryangel.managebuilding.com`.
  - Same PR also carried two earlier commits (merged separately as #246/#247): the Speed-Insights RES-tail fix (async Google Fonts + `preconnect` to images.unsplash.com) and removal of 5 orphaned `assets/photo-*` images.

- **Nave: full-history topic dedupe gate + structural-variety rules** (2026-07-19, branch `claude/blog-mechanism-review-3311it`): investigated an owner complaint that the newest post ("What's Your Bronx Tenant's August Move-Out Actually Costing You...") felt like a repeat of old ground, and that recent posts felt formulaic/"tiring" with too many numbers. Found two real gaps in `scripts/generate-post.js`:
  - **Weak duplicate-topic detection**: `pickTopicWithAI()` only showed the AI the last **10** post titles as "avoid repeating" (~3 weeks at the 2-day cadence), so older recurring themes (e.g. April's vacancy-cost posts vs. July's move-out-cost post) were invisible to the picker. Added `findSimilarPublishedTopic()` — a semantic Haiku dedupe gate checked against the **full** post history, mirroring Arlo's `findDuplicateOpenIssue()` in `daily-audit.js`. `main()` now retries the topic pick up to 2x with an explicit "that overlapped with X, pick something different" note when flagged, then proceeds anyway (fails open, same philosophy as Arlo) if still flagged.
  - **Every post following the identical skeleton**: reading the last 6 posts' section outlines showed all of them used the exact same shape (dollar-figure hook → bulleted cost breakdown → 2-3 rhetorical "### Is/Does/What Happens If...?" sub-headings → numbered checklist → "Bottom Line" closer) — a side-effect of `SYSTEM_PROMPT` rules mandating a dollar figure, E-E-A-T markers, and question-headings with no variety instruction. Added new rule 7 ("STRUCTURAL VARIETY") telling Nave to rotate shapes (scenario/story, myth-vs-fact, comparison table, plain conversational explainer with no checklist/no "Bottom Line") and use lists/checklists only when the content actually calls for them. Same fix applied to the manual `/blog-post-author` skill (`.claude/skills/blog-post-author/SKILL.md`), which had the old rigid skeleton hardcoded into its own instructions.
  - **Leaned into the winning comparison format** (owner confirmed this is the "one format performed way better" comparison they meant — Bronx-vs-Mount-Vernon/Manhattan style posts): added an explicit cadence target ("roughly 1 in every 3-4 posts") to the topic-picker prompt and `SYSTEM_PROMPT`, and added 2 more comparison-shaped fallback topics (Bronx vs. Yonkers, Bronx vs. New Rochelle) to `FALLBACK_TOPICS`.
  - Not changed: word-count/dollar-figure/question-heading rules themselves (still schema-valid per `POST_RULES.md`), the 2-day cadence, or the category-preference weighting.

- **Hero animated widget + owner-portal preview relocated into hero** (2026-07-19): two related additions built from Claude Design exports, iterated live with the owner via screenshots.
  - **Animated property-overview canvas widget** added to the DIY Landlord Toolkit (`#tools`) section, directly below the 4 tool cards (PRs #232, #233 for alignment fixes): a rounded-square canvas where 22 floating property-management icon bubbles (rent, repair, lease, tenant, inspect, urgent, invoice, vacancy, security, utility, cleaning, parking) plus the turtle mascot bounce with physics/collision, periodically snap into formations (circle → triangle → square → house), with a rotating tagline. Adapted from the raw export before adding: reused the existing `assets/logo-icon.jpg` instead of a duplicate inline base64 logo, swapped an off-brand gold accent for the brand blue palette, added `prefers-reduced-motion` support (CSS disables the shine/glow animations; JS renders one static frame instead of looping `requestAnimationFrame`), and made it responsive. Left-aligns with the lone 4th tool card at the desktop breakpoint (where `tools-grid`'s `repeat(3,1fr)` leaves it alone in column 1); recenters at ≤900px where the grid fills evenly with no card to align to.
  - **Owner-portal dashboard preview moved into the hero** (PRs #233–#235): **⚠️ SUPERSEDED 2026-07-22 (PR #248) — the iframe was removed from the hero and replaced by a lightweight rotating screenshot stripe in `#about`; see the 2026-07-22 entry at the top.** (Original note:) the "Transparent Reporting" intro + the `doryangel-owner-portal-reports` iframe embed — previously deep in the `#about`/Pillars section — now sits between the hero's CTA buttons and the stats row (15+/100+/24-7), per owner request. **This reverses the earlier "Removed: Owner/Tenant Portal card widget from hero" decision (PR #43) below** — see that line for the note. Trimmed to avoid duplicate messaging: cut the paragraph + 3-item checklist (repeated what the iframe's own header already says) and the "Transparent Reporting — Pillar 01" eyebrow label (redundant once the iframe's own "Investor Care · Owner Portal" label sits right below it) — only the heading remains as a one-line lead-in.
  - **`doryangel-owner-portal-reports` micro-site cleaned up too** (separate repo, `dror75p-ops/doryangel-owner-portal-reports`, PRs #1–#2): removed its own duplicate DoryAngel footer and logo/nav header, since both duplicated the real site's nav/footer once embedded via iframe on a page that already has them. **Note for future editors**: this repo's `index.html` is a Claude-Design-bundler export — components are stored as escaped-string blobs inside `<script>` tags (e.g. `</div>` for `</div>`, `</header>` for `</header>`), not plain literal HTML, so a plain-text search for a tag name won't find it directly; search for the element's distinguishing class/text instead and account for the `/`-escaped slashes when editing.
  - **Performance pass**: the portal-preview iframe now loads via JS (`document.createElement('iframe')`, appended after `window.load` + `requestIdleCallback`) instead of an eager `src`, since it's now above the fold and `loading="lazy"` doesn't help once an element starts in the initial viewport; `#portal-embed-outer` reserves its height up front so there's no layout shift when the iframe appears. The hero's NYC photo cross-fade (`#hero-skyline`) now only ships the first (visible) photo eagerly — the other 5 carry their `background-image` in a `data-bg` attribute and get it applied just-in-time by the existing 6s rotation interval, instead of all 6 being fetched on page load when only one is ever shown at once. Footer logo got `loading="lazy"`.
  - **Stability pass**: fixed a real broken link in the blog section's `<noscript>` fallback — it pointed to `how-much-will-spring-flooding-cost-bronx-landlords-in-2026-a`, a slug that no longer exists after the spring/summer duplicate-post consolidation; swapped to its current equivalent, `what-summer-flooding-in-your-bronx-building-could-cost-you-i`. Verified all internal `#anchor` links resolve, all local `src`/`href` file references exist on disk, and tag balance is intact.
  - Confirmed **no base64 images remain in `index.html`** — the old "compress the base64 logo" roadmap item (below) is resolved; removed from the roadmap/outstanding lists.
- **Nave title-formula tuning from doryangel.com traffic data** (2026-06-28): the owner's traffic analysis of the old doryangel.com showed the top posts were all **owner-targeted, problem-first** titles with a geo anchor + concrete number/cost ("How Much Is Inefficient Management Costing Your Bronx or Queens Rental?", "How to Screen Tenants in NYC Without Getting Sued", "Flat-Fee PM: What $99/Unit Actually Means", "What Should a Property Manager Actually Do for You?"). Generic, geo-less, marketing-speak titles got zero traffic. Folded this into Nave (`scripts/generate-post.js`):
  - **Topic-picker prompt + `SYSTEM_PROMPT` rule 1**: added the "winning title formula" (lead with the owner's pain/cost, address them as "you/your", include a number/$/law) with the 4 proven exemplars + the two zero-traffic anti-patterns.
  - **Geo rule relaxed**: Bronx stays the *primary* anchor, but an adjacent borough (Queens/Manhattan) is now allowed when natural — the #1 traffic post was "Bronx or Queens". Previously titles were strict Bronx-only.
  - **`FALLBACK_TOPICS`**: 4 property-management entries upgraded in place to the proven shapes (category counts unchanged).
  - ~~**Deliberately NOT changed (owner's call):** the category-preference bias still "strongly prefers" investments/automation/broker-partnerships...~~ **REVERSED 2026-07-11 (owner request):** the category-preference bias in `pickTopicWithAI` was inverted to be **owner-facing / property-management first**, matching the real search traffic (all June clicks were "property management bronx" owner queries). New rule: DEFAULT to `property-management` or `diy-property-management`; `investments` occasionally; `property-automation`/`broker-partnerships` only sparingly for a genuinely fresh angle. `FALLBACK_TOPICS` rebalanced to ~71% owner-facing (PM 10 / DIY 7 / investments 3 / automation 2 / broker 2). The existing 58-post catalog is still skewed (PM 18 / DIY 5 / investments 13 / automation 14 / broker 8) — the reweight fixes the *forward* cadence; the mix shifts over the next weeks as new posts land. Consolidating the near-duplicate automation/"5 summer X" posts is the remaining content cleanup if wanted.
    - **Comparison format added 2026-07-15 (owner request):** the owner's **Bronx vs. Mount Vernon** comparison was one of the best-performing posts, so both the topic-picker prompt and `SYSTEM_PROMPT` rule 1 now call out the **Bronx-vs-adjacent-area COMPARISON** as a proven winner and favor owner-facing self-manage-vs-hire angles. **Mount Vernon** (Westchester city bordering the Bronx) is allowed **only inside a Bronx-anchored comparison, never as the sole location**. Two comparison/self-manage-vs-hire entries added to the top of the DIY block in `FALLBACK_TOPICS`. (Category-bias reversal from 2026-07-11 left intact — this is additive.)
- **Security hardening + Turnstile + email rebrand** (2026-06-28): lead-pipeline security pass plus a brand polish of every transactional email. PRs #149 (Turnstile client + broadcast secret + slug fix), #150 (Web3Forms hotfix), #151 (Tools menu) — all merged to main.
  - **Cloudflare Turnstile on the lead forms**: widget added to the **contact**, **digest signup**, and **tax checklist** forms (`index.html`, `tax-checklist/index.html`). The challenge token is sent to Make as **`turnstileToken`** — a hyphen-free key, because Make's expression parser reads the hyphens in `cf-turnstile-response` as minus. **Server-side siteverify is ENFORCED on contact (6335620) + tax (6346876)**: an `http:ActionSendData` module POSTs `secret`+`response` to `challenges.cloudflare.com/turnstile/v0/siteverify`, and the downstream email/sheet modules are gated on `{{N.data.success}} = true`. Widget is registered for hostnames `www.doryangel.com` + `doryangel.com`. The **site key is public** (it's in the page HTML); the **secret key lives only in the Make HTTP module + Cloudflare** — not in the repo. Verified live on beta: both verified submissions deliver.
    - **Digest is deliberately NOT server-gated** (left client-only): its signup webhook (5549170) is shared with the external `dror75p-ops.github.io/...digest/` landing page, which has no Turnstile — hard-gating would silently drop those signups. Revisit if that page is retired or gets its own Turnstile.
    - **Turnstile errors on non-registered hosts** ("Unable to connect to website"): the widget only runs on its registered hostnames (+ localhost). It will error on Vercel previews / `github.io` until those hosts are added — test on `www.doryangel.com`.
    - **Web3Forms gotcha**: the Turnstile widget injects a `cf-turnstile-response` field into the form; Web3Forms tried to validate it against an unconfigured secret and returned `success:false`, breaking the contact form ("Error – please call"). Fix: `data.delete('cf-turnstile-response')` before the Web3Forms POST (contact + digest) so the field never reaches Web3Forms; it stays in the DOM for the Make token read.
  - **Broadcast webhook shared secret**: `scripts/generate-post.js` now sends a `secret` field (env `DIGEST_WEBHOOK_SECRET`, wired through `blog-autopublish.yml`); the broadcast scenario (6347243) filter requires it, so the public hook can't be abused to blast arbitrary emails. Secret value lives only in the GitHub Actions secret + the Make filter — never in the repo. NOTE: if the GitHub secret is missing/typo'd, the next blast silently sends to 0 (filter drops all).
  - **All transactional emails re-branded**: removed the off-brand gold `#F5C518` buttons across every Make email template (contact, digest welcome 5549170, digest broadcast 6347243, tax 6346876, compliance signup 5546593, compliance monthly 5546729, notify 6335621) → brand blue `#1E5AA8` buttons on light blue-dim (`#EBF3FD`) cards with navy (`#0F2847`) headings, on white. Repointed every CTA/footer link to `www.doryangel.com` (fixed `github.io` links, apex `doryangel.com`, and a `dryangel.com` typo). Only email HTML changed — no filters, mappers, the broadcast secret, or the monthly schedule were touched.
  - **Chat Leads filter fix (5578524)**: removed the `length()` + `number:greater` condition (the documented Make gotcha that silently evaluates false) — it was dropping ~90% of Hailey's chat leads (23 executions / 27 ops). Filter is now `event = chat_analyzed` only, matching the original documented design.
  - **generateSlug em-dash fix** (`scripts/generate-post.js`): dash variants (em/en/figure dashes, minus) are converted to spaces before stripping, so `tenants—smart` → `tenants-smart` (not `tenantssmart`); also trims leading/trailing hyphens, including after the 60-char truncation.
  - **Tools menu**: added the **Bronx Landlord Tax Checklist** (`/tax-checklist/`) and **Flat-Fee Savings Calculator** (`/flat-fee-vs-commission/`) to the desktop Tools dropdown + mobile drawer — they existed in the page body but weren't in the nav.
  - **Hiokj→Dror**: fixed the garbled name on dror75p's first digest-sheet row (done manually by the owner; still no Sheets-write tool available in-session).

  **Open / next steps (start here next session):**
  - **Digest Turnstile decision** — currently client-only. Options: gate it (breaks the external `github.io` signup page), keep client-only, or add Turnstile to that external page (separate repo, not in this session's scope).
  - **Owner dashboard tasks (not code, only the owner can do):** set hard **budget caps** on Make.com + Anthropic; **verify `doryangel.com` in Resend** (lifts the sandbox-domain limit). Optional: add the stable Vercel hostname to the Turnstile widget if you want it to render on Vercel previews.
  - Re-enable strict per-topic digest segmentation once the subscriber list is large enough (see Digest segmentation note below).
  - Arlo issue #117: Astoria-is-in-Queens factual error corrected in the issue body (Astoria removed; real Bronx neighborhoods listed); scope revised to 2–3 guides and acceptance criteria rewritten as concrete deliverables. Issue intentionally kept open as a scoped content task (the guides aren't written yet). Note: the live site never contained the error — Astoria only appears in Queens/Manhattan posts, correctly placed.

- **Digest hardening + ops** (2026-06-27): fixed a chain of bugs found while making the Digest blast actually deliver, plus a security pass.
  - **Sheet writes use `RAW`** (signup + tax-checklist `addRow`): blocks Google Sheets **formula injection** (a submitted `=...` name is stored as literal text, not executed).
  - **Topics must be `join()`-ed before the sheet**: a raw `topics` **array** in the addRow value caused `400 INVALID_ARGUMENT: list_value` and **auto-deactivated the scenario**. Mapper col C = `{{join(1.topics, ", ")}}`. The homepage form sends `topics` as an array (matches the external digest page).
  - **Make filter gotcha — avoid `number:greaterThan` / `length()`**: those conditions silently evaluated false and blocked EVERY valid submission (both the signup and the broadcast scenarios). Filters now use only `text:contain` / `text:notcontain`. Disposable-domain spam list restored that way.
  - **Broadcast relay hardened**: filter requires the post URL to `startwith https://www.doryangel.com/`, and `name`/`title`/`excerpt` are HTML-stripped via `replace()` → the public webhook can't be abused to send arbitrary-link phishing from `office@doryangel.com`.
  - **Gate forms debounce** (`index.html` tool-gate + notify-gate): submit button disabled on first click to stop webhook double-fire.
  - **Signup sheets stamp date + time** now (`YYYY-MM-DD HH:mm:ss`, Asia/Jerusalem) instead of date-only — both Digest + Tax Checklist.
  - Verified live: Nave run → post committed → broadcast emailed all active subscribers (dror75p, somerlev) → delivered. `joyg@doryangel.com` + `raphael.doryangel@gmail.com` added to the digest sheet by hand (sandbox can't reach the Make webhook to add via the form; no Sheets-write tool available).
- **Digest topic segmentation, end-to-end** (2026-06-26): subscribers now pick interests at signup and only receive matching posts.
  - **Signup**: the Digest tool-gate modal (`index.html`) shows a Digest-only topic picker (the 4 blog categories) and POSTs a `topics` array to the Make signup webhook → stored in the subscriber sheet's `topic` column.
  - **Send**: `scripts/generate-post.js` → `notifyDigestSubscribers()` emails **every active subscriber** (de-duped by email), delivering via a Make.com **Gmail** broadcast scenario — NOT Resend. The Resend sandbox domain (`onboarding@resend.dev`) only reaches the account owner, so the subscriber blast must go through Gmail (`office@doryangel.com`). Post links use canonical `www.doryangel.com`. NOTE (2026-06-27): switched from strict per-topic segmentation to **reach-first (send-to-all-active)** because the small list + Nave's preference for underrepresented categories (property-automation, broker-partnerships — which had zero subscribers) meant most blasts emailed 0 people. Topic preferences are still collected in the sheet (column C); re-enable the exact topic-slug match in `notifyDigestSubscribers()` once the list is large enough to benefit.
  - Also relaxed the Digest signup filter to accept **email-only** signups (name optional) — previously no-name signups were silently dropped from both the sheet and the welcome email.
- **Bronx Landlord Tax Deduction Checklist lead magnet** (2026-06-26): `tax-checklist/index.html` (landing) + `tax-checklist/checklist.html` (28 deductions). Form posts to ONE backend — the Make.com "Tax Checklist" scenario (welcome email + owner notify + Google Sheet log). The homepage entry is a 4th `.tool-card` in the DIY toolkit grid (not a banner). Web3Forms was removed from this form to stop duplicate owner emails.
- **Flat-fee vs. % calculator landing page** (2026-06-26): new `flat-fee-vs-commission/index.html` — interactive savings calculator (units × rent × commission %), comparison table, 3 real Bronx examples (Mott Haven / Fordham / Riverdale), FAQ + FAQPage/BreadcrumbList JSON-LD, and a Web3Forms lead-capture form. Calculator is anchored to published pricing (**$99 per unit per month, all portfolio sizes** — owner-corrected 2026-07-08; the old "$99/mo single unit, $199/mo for 2–10 units" total-price model was a misunderstanding) — do NOT invent other numbers. The 2026-07-08 consistency pass updated ALL old-model copy site-wide: homepage pricing cards (Starter + Professional both now $99/unit/mo), hero "$99/Unit. Period.", meta/OG/JSON-LD (priceRange "$99/unit/month"), the calculator page's comparison table / 3 Bronx example cards / FAQs, the announce bars ("Save $2,000–$8,000/year"), and the two blog posts that stated the old model (`self-managing-bronx-rental-vs-flat-fee-property-management`, `flat-fee-property-management-vs-percentage` — the latter's rent-tiered $99/$149/$199 fee table was flattened to $99). Linked from the pricing section of `index.html` (a styled CTA card) and added to `sitemap.xml`. Consolidates Arlo issues #119/#125/#126/#127.
  - **UPDATE 2026-07-12 (owner request, PR #211): homepage pricing is now a 3-tier display — Starter `$99/unit/mo`, Professional `$199/unit/mo`, Portfolio `Custom`.** Only the Professional *card* changed (Starter stays $99); the visible card now matches the Professional `Offer` already in the homepage JSON-LD (`"199"`). **Deliberately left at $99 (owner's call — scope limited to the card):** the hero "$99/Unit. Period.", the flat-fee calculator (`flat-fee-vs-commission/`, computes at $99/unit flat), the announce bar, meta/OG, and `priceRange "$99/unit/month"`. So $99 = the entry/Starter/advertised rate; $199 = the Professional tier only. **Do NOT "correct" the Professional card back to $99** — the $199 is intentional and supersedes the 2026-07-08 flatten for that one card. The known hero-vs-card inconsistency ("Period." vs a $199 tier) is accepted for now.
- ~~**Removed**: Owner/Tenant Portal card widget from the hero section (the tab + ledger mockup). Merged via PR #43. Reason: it distracted from the primary lead-collection CTA flow.~~ **PARTIALLY REVERSED 2026-07-19 (owner request):** a different owner-portal element — the `doryangel-owner-portal-reports` dashboard-preview iframe (not the old static tab+ledger mockup, which stays gone) — was deliberately added back into the hero, between the CTA buttons and the stats row. See the 2026-07-19 entry above for the full story and what was trimmed to keep it from competing too hard with the CTA flow. **⚠️ Update 2026-07-22 (PR #248): that hero iframe has since been removed — it was too heavy in the hero — and replaced by a lightweight rotating screenshot stripe in `#about`. So the hero no longer contains any owner-portal element; the portal preview now lives in `#about` after the pillars. See the 2026-07-22 entry at the top of "Last changes".** **⚠️ Update 2026-07-27 (PR #261): an owner-portal element is back in the hero once more — but as a plain static `<img>` (26KB WebP, eager, explicit dimensions), desktop-only at ≥1024px, NOT an iframe. That is the distinction that matters: the 2026-07-22 problem was an iframe loading an entire external site during the LCP window, not the imagery itself. Measured LCP with the screenshot in place is ~0.3s. The `#about` rotating stripe stays as-is.**
- **Accessibility fixes** (merged PR #43): contact form labels linked to inputs via `for`/`id`; FAQ buttons now have `aria-expanded`; hero photo divs have `role="img"` + `aria-label`; blog carousel has `<noscript>` static fallback.
- **Logo promo flip** (pending PR): hamburger button removed from the flip animation — now always static. Logo flips every 5 seconds (first at 5s after load), shows promo for 3s, then flips back.

### Hosting status (as of 2026-07-07 — DNS CUTOVER DONE)

- **Production**: **Vercel** serving **https://www.doryangel.com/** — DNS cutover completed 2026-07-07. The old Wix site is offline; the apex `doryangel.com` 308-redirects to `www.doryangel.com`. All canonicals/sitemap/OG and same-site links use `www.doryangel.com`. (Separate micro-app tools — Compliance Calendar, Digest landing, AI Property Inspector, owner-portal-reports — keep their own `dror75p-ops.github.io/...` URLs; they are NOT pages on www.doryangel.com.) The owner-portal-reports one is repo `dror75p-ops/doryangel-owner-portal-reports` — as of 2026-07-22 it is **no longer iframed** into this site; instead, static WebP screenshots of its 7 panels (`assets/portal-*.webp`) are shown in a rotating stripe in `#about` (see the 2026-07-22 entry in Last changes). Its own `index.html` is a Claude-Design-bundler export (component markup is stored as escaped-string blobs with `/`-escaped slashes, not plain literal HTML) — search by distinguishing class/text, not raw tag names, before editing it directly.
- The old `beta.` subdomain was the pre-cutover hostname (GitHub Pages). Every repo reference was migrated to `www.doryangel.com` on 2026-07-07 — do not reintroduce `beta.` anywhere.
- `vercel.json` carries the 301 redirect map for all 111 old Wix `/post/...` URLs plus the security headers; `.vercelignore` keeps dev artifacts (project/, chats/, scripts/, workflows, docs) out of the deploy.

### Post-cutover follow-ups (owner / dashboard tasks)

1. **Google Search Console** — ~~"Change of address" beta → www~~ **N/A (2026-07-24): no separate `beta` property; the `doryangel.com` Domain property already covers www, and host consolidation is CONFIRMED working — Google's indexed homepage resolves to `www.doryangel.com`, and beta/apex/http URLs in the Performance report are pre-cutover legacy that will fade on recrawl. Full detail + the 28-day performance review are in the 2026-07-24 "Last changes" entry.** Remaining action: **resubmit `sitemap.xml` to the `doryangel.com` Domain property + Request Indexing** on `/`, `/blog/`, and `/guides/bronx-landlord-compliance/`. ~~then re-pull the Performance export in 2–4 weeks~~ — **no longer a manual job (2026-07-31): Arlo pulls rankings from the Search Console API every morning and appends them to `project/seo/rank-history.json`.** Measured position for "bronx property management" is **~20**, not the ~12 this line used to claim.
2. **Resend** — verify `doryangel.com` as the sending domain (lifts the sandbox-domain limit; lets owner email send from the brand domain).
3. ~~**Make.com email templates**~~ — **DONE** (verified via Make API 2026-07-08: welcome 5549170, broadcast 6347243, and tax 6346876 templates all link to `www.doryangel.com`; repointed in the 2026-07-07 cutover session).
4. ~~**Broadcast scenario 6347243 filter**~~ — **DONE** (verified via Make API 2026-07-08: filter requires `https://www.doryangel.com/` prefix + shared secret; scenario active; matches `postUrl` in `generate-post.js:630`).
5. ~~**Turnstile widget hostnames** — ensure `www.doryangel.com` is registered~~ — **DONE** (the widget is registered for `www.doryangel.com` + `doryangel.com`, per the 2026-06-28 Turnstile entry above). Only add other hosts (e.g. a stable Vercel preview host) if you want the widget to render there.
6. **Auto-publish cadence bumped to every 2 days + re-enabled 2026-07-10** (`blog-autopublish.yml.disabled` → `.yml`; it had been disabled again after the 2026-07-08 re-enable). Cron is now 14:00 UTC on **odd calendar days** (`1,3,5,…,31`) instead of every-3-days `1,4,7,…`. Nave's SYSTEM_PROMPT already states the correct $99/unit/month pricing. **Instagram distribution stays MANUAL for now** (owner's call) — no IG API/token wired into Vera. Instead, Nave's approval email was relabeled "Facebook & Instagram" and STEP 3 now tells the owner to post the same saved image + copied caption to both (IG link isn't clickable → "link in bio"). If auto-IG is wanted later: Vera (`social-post.js`) needs a 2-step Graph publish (create media container → media_publish) plus an `INSTAGRAM_ACCOUNT_ID` secret and an IG-scoped token, and Nave must add `heroImage` to the `/tmp/social-queue.json` handoff (currently only `slug` + `facebookPost`).

## Workflow rules

**Push policy: branches + Pull Requests for any significant change.**
- Create branch `claude/<short-description>`
- Push commits there
- Open PR via `gh` / GitHub API
- Hand the PR URL to the user so they can review in the GitHub mobile app
- Wait for the user to merge before changes go live
- Exception: auto-publish workflow runs as the DoryAngel Bot and commits directly to main on its 2-day schedule
- Exception: trivial one-line tweaks if the user explicitly says "just push it"

## What's in this repo

- `index.html` — Full website (self-contained, all CSS inline, all JS inline at bottom)
- `guides/bronx-landlord-compliance/index.html` — **NYC Landlord Compliance Guide** pillar hub page (topic-cluster hub linking 9 compliance posts; standalone page on the sibling landing-page shell). Display name is "NYC" but the folder/slug keeps "bronx" — do not rename the slug. Template for any future pillar hubs (Comparison/DIY were parked).
- `blog-loader.js` — Renders blog cards on the home page; each card links to `/blog/[slug]/`
- `content/blog/posts-index.json` — Source of truth for all posts
- `scripts/generate-post.js` — Agent **Nave**: generates the post (Claude API → Unsplash) → writes `posts-index.json` → fires the topic-segmented digest broadcast (Make/Gmail) → Resend approval email to owner → hands a social queue to Vera. **(2026-07-24, PR #256)** now also weaves 2–3 contextual in-body internal links into each new post: `pickLinkablePosts()` offers the top-8 most-relevant existing posts + the compliance hub to the prompt (SYSTEM_PROMPT rule 15), and `sanitizeInternalLinks()` unwraps any hallucinated `/blog//guides/` link so it can't 404.
- `scripts/social-post.js` — Agent **Vera**: posts the new article to the Facebook Page (reads `/tmp/social-queue.json` from Nave). Does NOT touch subscribers. **(2026-07-28)** Posts **via the Make scenario**, not the Graph API — no Facebook token lives in this repo. Runs as the LAST workflow step (after commit) and waits for the post URL to go live before posting; never fails the workflow; emits `::warning::`/`::error::` annotations on a skip or failure; requires a post id in the webhook reply (a bare `Accepted` is a failure); emails the caption to the owner if the post fails. See the ⚠️ four invariants in Last changes before editing.
- `scripts/build-blog.js` — Generates per-post HTML pages from the JSON. **(2026-07-24, PR #255)** `getRelatedPosts()` now blends "Continue reading" across categories (2 in-cluster + ≥1 cross-cluster, scored by hashtag/word overlap) instead of same-category-only; posts in `GUIDE_HUBS` (the 9 compliance slugs) get a hub up-link; and `buildSitemap()` includes the `/guides/bronx-landlord-compliance/` URL (so Nave runs don't drop it).
- `scripts/migrate-posts-once.js` — One-time schema migration (kept for reference)
- `scripts/refresh-images.js` — One-shot workflow to refresh all post hero images
- `blog/[slug]/index.html` — Auto-generated per-post pages (NEVER hand-edit; rerun build-blog.js)
- `.github/workflows/blog-autopublish.yml` — Cron-triggered every 2 days at 14:00 UTC (odd calendar days)
- `.github/workflows/refresh-images.yml` — Manual one-shot for image refresh
- `sitemap.xml`, `robots.txt` — SEO
- `.gitignore` — node_modules, .env, .DS_Store, logs, /tmp/

## Brand / design

- Brand colors: navy `#0F2847`, blue `#1E5AA8`, blue-light `#5B9FEA`, blue-dim `#EBF3FD`
- Fonts: DM Serif Display (headings) + DM Sans (body) — Google Fonts
- Tone: professional, trust-first, NYC-rooted

## Hero animations (the "top part")

- **Living Turtle**: walks in on load, breathes on a 5s loop, pops on hover
- **Rotating headline** (rebuilt 2026-07-27): the H1's first two lines are **fixed**; only the third — the promise line — rotates, through **6 slogans every 6s** with a mask reveal plus a hand-drawn underline that redraws each swap. See the three ⚠️ invariants in the 2026-07-27 entry above before editing: slogans must fit one line at 320px, and `.reveal` must never go back on the H1.
- **Rotating trust bar**: first chip cycles every 4s through 5 proof points (the 5th, the 24/7 emergency-maintenance line, was added 2026-07-27 when the hero stat became "2 hr")
- **NYC photo cross-fade**: 6 bright NYC photos cycle every 6s at **~10%** opacity behind the hero (lowered from 22% on 2026-07-27 — at 22% they were a grey wash fighting the copy for contrast; the desktop portal screenshot now carries the visual). Only the first ships eager; the other 5 carry their `background-image` in a `data-bg` attribute and get applied just-in-time by the rotation interval, so they aren't all fetched on page load.
- **Desktop-only portal screenshot** (≥1024px): the hero is a split, with the real owner-portal screen on the right and a floating "Portfolio Health / 98%" card. Below 1024px the hero is the centred single column it has always been.
- **Sticky mobile call button**: hidden until the hero scrolls past, then bottom-right at `bottom:111px` — high enough to clear the Retell chat bar. Do not move it down.
- All animations honor `prefers-reduced-motion`

## Other site features

- Scroll-triggered fade-up reveals (IntersectionObserver) on hero, section titles, audience/pillars/pricing grids
- Hero stats count up from 0 when entering viewport
- Reviews carousel auto-scrolls every 5s when visible
- Cookie consent banner with Google Analytics 4 (`G-0W61NYHM78`) — only loads after Accept
- All animations + tracking fully GDPR-compliant

## Blog architecture (current)

- **5 categories**: `property-management`, `diy-property-management`, `investments`, `property-automation`, `broker-partnerships`
  - ⚠️ **`diy-property-management` is DISPLAYED as "Maintenance & Repairs"** (since 2026-07-26). The slug is legacy and deliberately unchanged — it is baked into published pages' `article:section`, the blog filter tabs, and the digest signup topic values. Display name ≠ slug, same arrangement as `/guides/bronx-landlord-compliance/` reading "NYC". **Do not rename the slug.** The label lives in three places that must stay in sync: `CATEGORY_LABEL` in `scripts/build-blog.js`, `CATEGORY_LABEL` in `blog-loader.js`, and the digest topic checkbox in `index.html`.
- **Per-post URLs**: `/blog/[slug]/index.html` — Google-indexable
- **SEO per post**: `<title>`, meta description, canonical, Open Graph, Twitter Card, JSON-LD BlogPosting schema
- **Featured post** flag on JSON for the larger card on the index
- **Post page** has: full-width hero image, sticky CTA, markdown body, CTA block, "Continue reading" related posts (3 from same category)
- **Auto-publish workflow** (`blog-autopublish.yml`, every 2 days): Nave (`generate-post.js`, also sends the topic-segmented digest) → `build-blog.js` → commit all to main → Vera (`social-post.js`, Facebook via Make). **Vera is deliberately LAST** (since 2026-07-28) so the post page is already committed and deploying when the Facebook link goes out; it also polls the URL before posting. Do not reorder it above the commit. The digest broadcast needs no new secret (the Make webhook is a public URL); it reads subscribers via `GOOGLE_SA_KEY` (or public CSV fallback). Note: the digest fires before the post page is committed/deployed, so a clicked link can 404 for the ~1–2 min until Vercel redeploys — pre-existing, low-impact.

### Post schema (current)

```jsonc
{
  "slug": "...",
  "title": "...",                  // question OR numbered, with city anchor
  "category": "property-management | diy-property-management | investments | property-automation",
  "excerpt": "...",                // pain-point focused, 1-2 sentences
  "publishedDate": "2026-04-27",   // ISO date
  "minutesToRead": 5,
  "heroImage": "https://images.unsplash.com/...",
  "heroImageAlt": "...",           // required for SEO/a11y
  "hashtags": ["...", "..."],
  "featured": false,               // only one post should be true
  "seoTitle": "...",               // ≤60 chars, ends in "| DoryAngel"
  "seoDescription": "...",         // ≤155 chars
  "author": "DoryAngel Team",
  "content": "..."                 // markdown body
}
```

### Content rules (data-validated by real traffic)

- Title format: question OR numbered list. **Winning formula (validated by real doryangel.com traffic):** problem-first — lead with the OWNER'S pain/cost and address them directly ("your", "you"), e.g. "How Much Is Inefficient Management Costing Your Bronx or Queens Rental?". Owner-targeted beats broker/tenant-targeted; service-description titles ("What to Look for in a PM Company") underperform.
- Geo anchor: Bronx is the **primary** location, but a second **adjacent** borough (Queens/Manhattan) is allowed when natural (e.g. "Bronx or Queens") — the top traffic post used exactly that. A **Bronx-vs-adjacent-area comparison** is a proven high-traffic format (e.g. **Bronx vs. Mount Vernon**); Mount Vernon (Westchester city bordering the Bronx) is allowed **only inside a Bronx-anchored comparison, never as the sole location**. Never a non-Bronx borough/city alone. (Geo rule relaxed 2026-06-28; comparison/Mount Vernon added 2026-07-15.)
- Word count: 800-1,200 (below 500 = zero traffic)
- NYC-specific dollar figures or law/borough references required
- Tone: warm, caring, and reassuring toward the homeowner (a trusted neighbor who has their back) — while still expert, trustworthy, and practical. Lead with a touch of empathy, keep it grounded in specifics (fluffy feel-good copy gets zero traffic).
- No CTA in the body — auto-appended by build-blog.js
- Don't write generic AI-sounding pieces ("Power of Transparent Management Practices", "Maximizing Returns with Management Investment Strategies" — both got zero traffic)

## Key integrations

- **Anthropic API** (`ANTHROPIC_API_KEY` secret): `claude-opus-4-7`, structured outputs, prompt caching
- **Resend** (`RESEND_API_KEY` secret): from `onboarding@resend.dev` — **owner-facing emails only** (Nave's approval email, daily-audit report) go to `dror75p@gmail.com`. The sandbox domain can ONLY reach the account owner, so it is NOT used for subscriber-facing email. Verify doryangel.com in Resend to lift this.
- **Unsplash** (`UNSPLASH_ACCESS_KEY` secret): per-category curated queries for cover images
- **GitHub** (`GH_TOKEN` secret): the bot's token for committing to main; needs `repo` + `workflow` scopes
- **Facebook** (via **Make**, not the Graph API): Vera POSTs the caption + link to the Make webhook `ura1w6nxwhdgbkg0ebbdu6fwiuq4zi38`, and the **"DoryAngel Blog — Facebook Auto-Post"** scenario publishes it using Make's OAuth Facebook Pages connection. **There is deliberately no Facebook token in this repo** — Make holds and refreshes the connection, so nothing expires. Auth is the shared `DIGEST_WEBHOOK_SECRET`, enforced by a filter in the scenario. The old `FACEBOOK_PAGE_ID` / `FACEBOOK_PAGE_ACCESS_TOKEN` secrets are unused and can be deleted. Instagram has no API wired in and stays manual.
- **Google Analytics 4** — **dual-tagged** (2026-07-15): `index.html` now sends to BOTH `G-0W61NYHM78` (primary, post-move) and `G-P8QR4VL8NH` (legacy). Context: the www.doryangel.com move (PR #79, 2026-06-16) switched the ID from `G-P8QR4VL8NH` → `G-0W61NYHM78`, which made the old "doryangel.com" GA4 property (still what the owner opens by habit) flatline at the move date while all new data landed in the new property. Dual-tagging via a second `gtag('config', …)` restores data to the legacy property so nothing's lost — both properties now collect. Uses Consent Mode v2 (loads on every visit, storage denied by default → cookieless pings; full data after Accept); the single `gtag.js` load + global consent calls cover both IDs. Events (`generate_lead`, `tool_gate_submit`) auto-send to both.
- **Retell AI + Make.com lead capture**: see section below

### Make.com scenarios (team 998919, org 6531616, eu1)

All subscriber-facing email is sent here via the **Gmail** connection (`office@doryangel.com`, conn 5113797) — reliable to any inbox. The **Google** connection (`office@doryangel.com`, conn 5113631) writes the sheets.

| Scenario | ID | Webhook (`hook.eu1.make.com/…`) | Does |
|---|---|---|---|
| DoryAngel Digest — New Subscriber | 5549170 | `2qo6r1tr15cckv1opo8em9x89ip3ku4x` | signup → sheet `1-9IDAD1…` + welcome email. Filter accepts **email-only** (name optional). Maps `topics` (col C). |
| DoryAngel Digest — New Post Broadcast | 6347243 | `rbh91p9c72r0qypeuhmjvlsey3hutzgr` | one POST per matched subscriber from `generate-post.js` → Gmail sends them the new post. **This is the recurring digest send.** |
| DoryAngel Tax Checklist — Download & Welcome Email | 6346876 | `l1ydothnj57j2ngk6952oam9wds2wgk1` | checklist signup → welcome email + owner notify + sheet `1KbVggQ…` (tagged `Tax Checklist`) |
| DoryAngel Chat — New Leads | 5578524 | (Retell) | see Retell section below |
| DoryAngel Blog — Facebook Auto-Post | 6735249 | `ura1w6nxwhdgbkg0ebbdu6fwiuq4zi38` | Vera POSTs `{secret, slug, message, link}` → Facebook Pages **Create a Post** → Webhook Response returns the post id. Filter requires `DIGEST_WEBHOOK_SECRET`. **Uses Make's OAuth Facebook connection — no token in the repo.** |

Subscriber sheets are owned by `dror75p@gmail.com`, shared with `office@doryangel.com`. Digest subscribers: sheet `1-9IDAD1VmlnCvTdU3JqDWahjEFQaUFtRG-WayHZ9N8o` (cols: name, email, topic, date, active, address).

## Retell AI lead capture

AI **web chat** agent "Hailey" ("Doryangel web chat lead", agent ID `agent_88fd2bc14215e7210629dfafda`) is embedded on the website via the Retell chat widget. When a chat ends and is analyzed, Retell fires a `chat_analyzed` webhook to Make.com which logs the lead to Google Sheets and emails the owner.

The mobile "Call AI" button (`tel:+15167743249`) is a separate voice channel and is NOT connected to this Make.com scenario.

### Retell chat widget (index.html)

Embedded just before `</body>` (upgraded to **widget v2** — title "Ask anything", bubble bottom-left on mobile):
```html
<!-- Retell AI Chat Widget — Hailey lead capture (v2) -->
<script
  id="retell-widget"
  src="https://dashboard.retellai.com/retell-widget-v2.js"
  type="module"
  data-public-key="public_key_274f92a0516ded273d147"
  data-agent-id="agent_88fd2bc14215e7210629dfafda"
  data-title="Ask anything"
  data-color="#1E5AA8"
  data-bot-name="Hailey"
  data-popup-message="Hi! I'm Hailey — ask me anything about DoryAngel's property management."
  data-show-ai-popup="true"
  data-show-ai-popup-time="8"
  data-chat-bubble-position="left"
></script>
```

**Important**: Retell simulation mode does NOT fire webhooks. Only real chat sessions (widget on the live site) fire real `chat_analyzed` webhooks.

**Public-key domain allowlist gotcha (hit 2026-07-07, post-DNS-cutover)**: the Retell public key has an allowed-domains list in the Retell dashboard. If the site's hostname isn't on it, `POST api.retellai.com/create-chat` returns `401 "Public key is not allowed for this domain"` — the widget still renders but shows generic "Retell" branding (no Hailey title/greeting) and silently ignores messages, and NO webhook (not even `chat_started`) reaches Make. This killed chat after the www.doryangel.com cutover until the owner added `www.doryangel.com` to the key's allowed domains. If the site's hostname ever changes again, update that allowlist FIRST (same class of issue as the Turnstile hostname registration). Diagnosed by driving the live site with headless Chrome in a GitHub Actions runner (the CCR sandbox's network policy can't reach retellai.com or the live site); the throwaway harness was `scripts/retell-debug.mjs` + `.github/workflows/retell-debug.yml` on branch `claude/rettel-agent-repo-chat-hfk7rf` — removed after verification, recover from that branch's history if needed.

**Widget v2 mount point**: `retell-widget-v2.js` mounts a plain `<div id="retell-widget-root">` — NOT the v1 `<retell-chat-web-component>` custom element. The shadow-DOM viewport-fix script in `index.html` still targets the v1 element name, so it's currently dead code (harmless; remove or retarget whenever that area is next touched).

### Architecture

```
Retell chat ends + analyzed
  → webhook POST to Make.com (event_type = "chat_analyzed")
  → filter: chat_analyzed only (chat_started and chat_ended are dropped)
  → Google Sheets: append row to "DoryAngel Chat Leads" spreadsheet
  → Gmail: notify dror75p@gmail.com
```

### Make.com scenario

- **Name**: DoryAngel Chat — New Leads (scenario ID 5578524)
- **Webhook URL**: `https://hook.eu1.make.com/ii1kuc8ba6gk3yvs506ggrnutp6iwz4h`
- **Google Sheets**: spreadsheet ID `1druOTrJhRVhrbAPrGBWD8HtMNkxy-h-PiJcYEhGsuHk`, tab `Sheet1`
- **Both connections**: `office@doryangel.com` Google account

### Sheet columns (A–O)

All 14 lead fields are configured in Retell as **Post-Chat Data Extraction**. For a **chat agent**, the correct Make.com path is `{{1.chat.chat_analysis.custom_analysis_data.FIELD}}` — NOT `call.call_analysis.*`.

| Col | Field | Make.com expression |
|-----|-------|---------------------|
| A | Timestamp | `{{now}}` |
| B | caller_role | `{{1.chat.chat_analysis.custom_analysis_data.caller_role}}` |
| C | full_name | `{{1.chat.chat_analysis.custom_analysis_data.full_name}}` |
| D | callback_phone | `{{1.chat.chat_analysis.custom_analysis_data.callback_phone}}` |
| E | email_address | `{{1.chat.chat_analysis.custom_analysis_data.email_address}}` |
| F | property_address | `{{1.chat.chat_analysis.custom_analysis_data.property_address}}` |
| G | unit_details | `{{1.chat.chat_analysis.custom_analysis_data.unit_details}}` |
| H | is_occupied | `{{1.chat.chat_analysis.custom_analysis_data.is_occupied}}` |
| I | current_mgmt | `{{1.chat.chat_analysis.custom_analysis_data.current_mgmt}}` |
| J | is_urgent | `{{1.chat.chat_analysis.custom_analysis_data.is_urgent}}` |
| K | appointment_set | `{{1.chat.chat_analysis.custom_analysis_data.appointment_set}}` |
| L | chosen_slot | `{{1.chat.chat_analysis.custom_analysis_data.chosen_slot}}` |
| M | chat_summary | `{{1.chat.chat_analysis.custom_analysis_data.chat_summary}}` |
| N | chat_successful | `{{1.chat.chat_analysis.custom_analysis_data.chat_successful}}` |
| O | user_sentiment | `{{1.chat.chat_analysis.custom_analysis_data.user_sentiment}}` |

### Filter

Only `chat_analyzed` events trigger the pipeline (`chat_started` and `chat_ended` are dropped). This guarantees one row per chat with all post-chat extraction fields populated.

### Gmail notification

- **To**: `dror75p@gmail.com`
- **Subject**: `New Chat Lead — {{1.chat.chat_analysis.custom_analysis_data.full_name}} ({{1.chat.chat_analysis.custom_analysis_data.caller_role}})`

### History of bugs fixed 2026-05-06

1. **Column offset**: mapper keys were 1-indexed instead of 0-indexed, so Timestamp landed in column B and everything was shifted right. Fixed.
2. **Wrong data source path**: Fields were being read from `retell_llm_dynamic_variables.*` — but this agent uses Post-Chat Data Extraction, which writes to `custom_analysis_data.*`. Fixed.
3. **Duplicate rows**: Filter passed both `call_ended` and `call_analyzed`, creating 2 rows per chat. Fixed: `chat_analyzed` only.
4. **Wrong event name**: Filter used `call_analyzed` but chat agents fire `chat_analyzed`. Fixed.
5. **Wrong payload root key**: Make.com paths used `1.call.call_analysis.*` but chat agent payloads use `1.chat.chat_analysis.*`. Confirmed via 4-path debug email test — Path A (`chat.chat_analysis`) had all real data. Fixed.

### History of bugs fixed 2026-06-23

6. **Built-in analysis fields pathed one level too deep**: `chat_summary` (col M), `chat_successful` (col N), and `user_sentiment` (col O) were mapped to `1.chat.chat_analysis.custom_analysis_data.<field>` — but these three are Retell **built-in** analysis fields, siblings of `custom_analysis_data`, not children. They returned null on every row, so column N (the success signal) was always blank and Hailey's success rate was unmeasurable. Fixed in both the Sheets mapper and the Gmail body to `1.chat.chat_analysis.<field>` (no `.custom_analysis_data`). The 14 custom-extraction fields stay under `custom_analysis_data` — only these three reserved built-ins moved. Verify: run one real chat that gives a name + phone/email; column N should now read TRUE/FALSE.

## Cost (per blog post)

~$0.02-0.05/post (Opus 4.7), ~$2-3/month total

## Status (as of 2026-06-17)

This repo is now the **primary active design** and will replace the Wix website in the coming days.
- Hamburger mobile nav is live (merged PRs #11, #12, #13)
- Auto-publish blog pipeline running every 2 days
- Daily website audit automation live (PR #87) — runs every 9 AM EST, auto-fixes + creates GitHub Issues (agent **Arlo**, `scripts/daily-audit.js`). Since 2026-07-31 he also reports **Google Search Console rankings** and logs them to `project/seo/rank-history.json` — four data sources now (GA4, Make/Sheets leads, Clarity, Search Console).
  - **Hard dedupe gate (2026-07-15):** after Arlo generates an idea it runs a **semantic** Haiku check (`findDuplicateOpenIssue()`) against the open `automation`-labeled issue titles and **does not file** if the idea is substantially the same work as an existing one (any pillar/hub/internal-linking idea dupes any other; any cost/ROI/savings calculator dupes any other). Fails open (files) on error. Replaces the old prompt-only "avoid duplicating these" instruction, which was unreliable — the model kept re-proposing the same cost-hub/calculator idea in different words (issues #176/#188/#199/#221 all restated #185). The digest email still sends on a deduped day, noting which issue it overlapped.
- Microsoft Clarity live (ID `x7y1bk4fhc`, consent-gated)
  - **Nave reads a Clarity engagement signal** (`getClarityCategorySignals()` in `scripts/generate-post.js`, added 2026-07-11, secret `CLARITY_API_TOKEN`): pulls the last-3-day per-blog-URL scroll depth/engagement, maps each URL to its post category, and passes it to the topic-picker as a **tiebreaker only** (explicitly told never to override the owner-facing category preference). Fails soft — returns `null` (no signal) if the token is missing, the API errors, or no per-category data comes back, and the picker runs fine without it.
  - **Why the signal is often empty right now (do NOT misdiagnose as a bug):** the site moved to **Vercel / `www.doryangel.com` on 2026-07-07** (platform transfer off the old host). Clarity data is tied to the live site, so the rolling 3-day export sits on top of a fresh cutover and is naturally **thin/settling**. Expect the Clarity signal to stay null or sparse for a couple of weeks post-move; only treat an empty signal as a real problem once post-Vercel traffic has had time to accumulate. (Current null path logs nothing — adding a one-line diagnostic to the token-missing / API-error / 0-categories branches is the cheap way to tell "thin data" from a real mismatch when it's next investigated.)
- LocalBusiness + RealEstateAgent JSON-LD schema live in `<head>`
- Client testimonials section added (PR #85)
- GitHub Pages live at the URL above; custom domain switch pending

## Improvement roadmap (exposure / SEO)

Priority order for next sessions:

### 1. Custom domain
- Point doryangel.com → GitHub Pages (CNAME record + Pages settings)
- Verify doryangel.com in Resend so approval emails go to office@doryangel.com
- Update canonical URLs and sitemap.xml once domain is live

### 2. Content velocity
- Import the 10 priority posts from the developer guide (real-traffic-tested topics)
- ~~Consider bumping auto-publish to every 2 days (change cron in blog-autopublish.yml)~~ ← **DONE 2026-07-10** (odd calendar days)
- Add an `investments` and `diy-property-management` featured post (currently likely missing)

### 3. Technical SEO
- Submit sitemap to Google Search Console (already verified)
- ~~Add `LocalBusiness` + `RealEstateAgent` JSON-LD schema to index.html~~ ← **DONE** (in `<head>`)
- ~~Add breadcrumb JSON-LD to blog post pages~~ ← **DONE** (build-blog.js emits `BreadcrumbList` on every post)
- ~~Internal linking / topic clusters~~ ← **DONE 2026-07-24** (PRs #253–#256: compliance pillar hub at `/guides/bronx-landlord-compliance/`, cross-cluster related posts on all 47 posts, hub up-links, and Nave auto-links new posts). Comparison/DIY pillar hubs still available to build from the same template.
- Add `hreflang` if Spanish content is ever added (large Bronx audience)

### 4. Conversion / CRO
- A/B test hero headline: rotating vs. static (measure via GA4 events)
- Add a lead-capture form or exit-intent popup for free audit CTA ← **DONE 2026-05-18** (Elfsight FOMO widget added)
- ~~Add social proof counters (Google review count, years in business) above the fold~~ ← **DONE 2026-07-27** (PR #261: the 5.0 ★ / 21 Google reviews badge now sits in the hero between the CTAs and the stats; "15+ years" was already in the stats row)
- ~~Add Microsoft Clarity heatmaps + session recording~~ ← **DONE** (tracking ID `x7y1bk4fhc`, consent-gated via `loadClarity()` in the cookie script)
- **"Free Rental Analysis" CTA test** — that phrase is the category-standard front door for owner traffic on competitor PM sites; ours says "Get My Free Property Audit". Worth an A/B test (surfaced during the 2026-07-27 hero research).

### 5. Performance
- ~~Compress the base64 logo image in index.html — it is ~80KB inline and delays first paint~~ ← **DONE** (verified 2026-07-19: no base64 images remain in `index.html`; logo is referenced from `assets/logo.jpg` / `assets/logo-icon.jpg`)
- ~~Add `loading="lazy"` to below-fold images~~ ← **DONE** (verified 2026-07-19: all below-fold `<img>`s have it; footer logo was the one gap, fixed)
- Consider splitting the single 2,400-line index.html into partials built at deploy time

### 6. Off-page / distribution
- Set up Google Business Profile posts linked to each new blog post
- Auto-share new posts to LinkedIn / Facebook (extend generate-post.js)
- Build backlinks: submit to NYC landlord forums, Bronx community boards, BiggerPockets

## Outstanding / nice-to-have

- Verify doryangel.com in Resend so emails can go to office@doryangel.com directly
- Import the 10 priority posts from the developer guide (real-traffic-tested topics)
- ~~Custom domain (doryangel.com) for the new repo if/when ready~~ ← **DONE** (DNS cutover completed 2026-07-07, see Hosting status above)
- ~~Compress the base64 logo (largest single byte cost in index.html)~~ ← **DONE** (see Performance roadmap above, verified 2026-07-19)

## Previous versions / reference

- Doeryangelweb.2 — design prototype (in `project/` folder of this repo)
- dror75p-ops/Doryangel-website — original main website repo
- dror75p-ops/Doryangel.web.google.studio.v3 — earlier version
